# ResSim TODO

`TODO.md` is the active execution tracker. Completed work has been moved to `docs/DELIVERED_WORK_2026_Q1.md`.

## Now

- [ ] Replace the current IMPES timestep path with a fully implicit black-oil FIM path in the Rust core.
  - Active convergence investigation now lives in `docs/FIM_CONVERGENCE_WORKLOG.md` instead of this tracker.
  - Current high-priority follow-up: the Rust-core SPE1/FIM gas path now has a focused regression, the no-op timestep-acceptance bug in `fim/newton.rs` is fixed, step 1 of the CPR follow-up has landed as a Schur-style well-aware coarse pressure correction, step 2 strengthens the extracted pressure solve in `fim/linear/gmres_block_jacobi.rs`, step 3 replaces the old inverse-entry extractor with a local Schur-style quasi-IMPES restriction/prolongation, and step 4 exposes CPR diagnostics in the verbose linear/Newton trace. The new retry classifier is also in. Focused CPR/Newton/SPE1 regressions still pass, and the corrected representative `wf_bt_12x12x3` trace now shows the remaining retry shelf is dominated by `nonlinear-bad` failures (`11` linear-bad vs `83` nonlinear-bad in the first outer step) even while the coarse stage reduces the extracted-system residual to machine precision, so the next leverage is now outer-step policy and/or nonlinear acceptance around the near-stagnant reservoir-row shelf rather than more raw CPR coarse-solver strength.
  - Current status: diagnostic phase has isolated the dominant failure to explicit well/perforation unknowns drifting away from the frozen-cell-consistent local well manifold, with 3D water breakthrough showing the same pattern as the earlier 2D water and gas cases.
  - March 2026 solver status: a first well-variable trust-region slice is now in place in `src/lib/ressim/src/fim/state.rs`. It removes perforation-row dominance from the water-breakthrough retries and suppresses the catastrophic gas well-manifold failures, leaving a cleaner remaining bottleneck in the reservoir cell equations and smaller well-row stagnation.
  - Latest Newton status: `src/lib/ressim/src/fim/newton.rs` now accepts near-converged retry entry states on residual before forcing another linear solve, and it rejects damped candidates that do not reduce the residual. This removes the old pattern where tiny-step gas retries were kicked from a good iterate into a worse well-row state.
  - Adaptive timestep controller: `src/lib/ressim/src/step.rs` now uses Newton-iteration-aware growth (easy steps grow 2×, hard steps barely grow), post-step saturation/pressure change limiting (ΔSw target 0.2, ΔP target 200 bar), and Newton-reported cutback factors instead of fixed 0.5 halving. On the `sweep_areal` 21×21×1 waterflood, breakthrough-window substeps dropped from 297 to 163 (45% reduction). Gap analysis and design rationale in `docs/FIM_CONVERGENCE_WORKLOG.md`.
  - Direct-migration constraint: do not keep a long-lived production IMPES/FIM split; temporary scaffolding is acceptable only while the migration branch is incomplete.
  - Detailed implementation checklist and proposed Rust module/API layout: `docs/FIM_MIGRATION_PLAN.md`.
  - Current implemented foundation slice: `src/lib/ressim/src/fim/state.rs` and `src/lib/ressim/src/fim/flash.rs` now exist, and `pvt.rs` / `mobility.rs` expose explicit-state helper APIs for future FIM assembly work.
  - Current implemented assembly slice: `src/lib/ressim/src/fim/scaling.rs` and `src/lib/ressim/src/fim/assembly.rs` now define cell-major unknown/equation ordering, equation/variable scaling, and a compileable coupled-system container ready for real residual/Jacobian contributions.
  - Current implemented solve slice: `src/lib/ressim/src/fim/linear/` now defines the FIM-specific linear-solver interface and sparse-LU debug backend, and `src/lib/ressim/src/fim/newton.rs` provides a compileable Newton/reporting scaffold around the new coupled-system API.
  - Current implemented cutover slice: `src/lib/ressim/src/step.rs` now has an explicit FIM branch behind a simulator mode switch, `src/lib/ressim/src/fim/state.rs` writes accepted FIM states back into the live simulator arrays, and the accepted FIM path now reuses the normal reporting/history pipeline. The FIM branch now records step reports from the accepted FIM well state itself rather than reconstructing rates through the legacy well-control logic, so benchmark diagnostics see solved perforation/BHP unknowns instead of reporting artifacts. Public-step regression coverage now exercises that branch for a closed system, a simple injector/producer case, and default-path rate-controlled reporting through the normal `step()` API. FIM is now the default production path; IMPES remains available only as an explicit opt-out while the remaining public-step parity gaps are closed.
  - Current implemented accumulation slice: `src/lib/ressim/src/fim/assembly.rs` now assembles closed-system black-oil component accumulation residuals in surface-volume units with a block-diagonal finite-difference Jacobian, and `src/lib/ressim/src/fim/newton.rs` now applies damped bounded updates instead of stopping at the linear solve.
  - Current implemented flux slice: `src/lib/ressim/src/fim/assembly.rs` now adds intercell water/oil/gas component fluxes in surface-volume units using iterate-state mobilities, capillary/gravity potentials, and upstream `Bo`/`Bg`/`Rs`, and it now assembles the transport coupling with explicit face-local derivatives rather than a broad finite-difference flux stencil.
  - Current implemented well-model review slice: `src/lib/ressim/src/fim/wells.rs` now isolates the FIM well/source model and switches it to a geometric Peaceman well index multiplied by iterate-state mobility, rather than reusing a static mobility-baked PI from committed state. Producer phase split and gas/oil surface conversion in the FIM path are now iterate-aware.
  - Current implemented control slice: rate-controlled injector and producer groups now carry explicit BHP unknowns inside `src/lib/ressim/src/fim/state.rs`, and `src/lib/ressim/src/fim/wells.rs` / `src/lib/ressim/src/fim/assembly.rs` now add group control residual equations so those BHPs are solved implicitly rather than root-found outside the Newton system.
  - Current implemented completion-topology slice: `src/lib/ressim/src/fim/wells.rs` now builds explicit FIM perforation records with resolved cell indices, and the FIM assembly consumes those perforations instead of scattering raw `idx(i,j,k)` lookups. This strengthens completion-to-cell connectivity checks and is the base for later perforation-level equations.
  - Current implemented explicit-well slice: `src/lib/ressim/src/fim/wells.rs` now groups repeated completion entries into explicit physical-well topology objects using the current vertical-well frontend pattern (`injector`, `i`, `j`, `bhp`, radius, skin), `src/lib/ressim/src/fim/state.rs` now carries one BHP unknown per physical well plus one flow unknown per perforation, and `src/lib/ressim/src/fim/assembly.rs` now assembles explicit well-constraint rows and perforation-flow rows. The anchor well-control equation is now a complementarity residual rather than a target-achievable heuristic branch, so rate/BHP switching is solved inside Newton from BHP slack and rate slack.
  - Physics fix folded into the FIM state layer: `src/lib/ressim/src/fim/state.rs` now reconciles undersaturated→saturated regime transitions conservatively instead of dropping excess gas inventory when `rs` crosses the local saturated value.
  - Current implemented schema slice: payload/runtime now support explicit `wells` definitions with stable IDs, grouped completions, and per-well schedule metadata, and the Rust `Well` model now carries `physical_well_id` so FIM topology groups by explicit identity instead of guessing from completion properties.
  - Current implemented multi-well control slice: the worker now pushes explicit per-well schedules into Rust by stable physical-well ID, the Rust `Well` model persists that schedule metadata, the FIM path resolves control equations per physical well instead of sharing one family-level injector row and one family-level producer row, and the legacy IMPES/runtime control and reporting path now uses the same explicit physical-well schedule semantics. Mixed producer/injector schedules can now coexist end-to-end without collapsing back to a single family target.
  - Current implemented linear-solver slice: `src/lib/ressim/src/fim/linear/` now includes a restarted GMRES backend with a block-diagonal preconditioner keyed to the cell-major FIM unknown layout, and the default `FgmresCpr` target now uses a projected pressure stage with block-based restriction/prolongation before the global smoother rather than falling straight back to sparse LU while CPR remains incomplete.
  - Current implemented Jacobian slice: `src/lib/ressim/src/fim/assembly.rs` now assembles exact well/perforation Jacobian couplings for source rows, well-control rows, and perforation equations, exact local accumulation Jacobian blocks, an explicit face-local transport Jacobian built from relperm/PVT/capillary/gravity sensitivities, exact local perforation-equation cell derivatives derived from relperm/PVT sensitivities, and exact producer source/control neighborhood derivatives for the sampled surface-fraction terms. The old local-FD hooks have been removed from the production assembly path; the only finite-difference logic left in this module is test-only oracle code used to regression-check the explicit analytical blocks.
  - Current implemented preconditioner slice: the default iterative FIM path in `src/lib/ressim/src/fim/linear/gmres_block_jacobi.rs` now applies a pressure-first two-stage correction using a projected reduced pressure system built from block-based restriction/prolongation and an ILU-style pressure-block factorization with defect correction, followed by a global block solve so transport/well unknowns respond to the corrected pressure field. It is still below a full CPR implementation, but it is now materially closer to the intended CPR/ILU-class path than the earlier pressure-relaxation bridge.
  - Benchmark-driven parity status: a new IMPES-vs-FIM hydrostatic gravity benchmark now passes on the default FIM path, which is a useful baseline that the no-well gravity branch is behaving consistently. Two additional benchmark diagnostics remain intentionally ignored because they still expose real FIM parity gaps rather than test-harness issues: an early-time Buckley waterflood comparison now advances on FIM but still shows a material saturation-profile mismatch, and a simple rate-controlled producer/injector comparison now produces nonzero FIM flow but still differs materially from IMPES. Separately, large frontend reference/benchmark runs (for example the 96-cell Buckley case behind the reference sweep UI) were found to stall on the first FIM timestep; that hardcoded benchmark-runtime fallback has now been replaced with explicit `fimEnabled` control in the scenario/benchmark definitions themselves, so known-problematic Buckley-style cases opt out locally instead of the runtime unconditionally forcing all benchmark runs off FIM.
  - Current wasm runtime blocker: the Buckley-style FIM startup stall is now narrowed to wasm-only timestep fragmentation rather than a generic frontend or reporting bug. A reproduced 24-cell wasm waterflood takes about 4,132 accepted FIM substeps to advance a single `0.25 d` external step, while native release completes the analogous FIM steps without that fragmentation. Linear-backend changes alone did not remove that wasm-only cutback spiral, so the remaining direct-fix work is in the nonlinear/update-acceptance path rather than simple payload/runtime wiring.
  - Remaining physical gap before production use: the FIM path now has explicit physical-well/perforation equations, per-physical-well complementarity control rows, explicit payload/runtime well identity, end-to-end explicit schedule semantics, a pressure-first iterative block preconditioner with projected pressure restriction/prolongation and an ILU-style pressure solve, analytical/local sparse Jacobian coverage for accumulation, intercell transport, and well/perforation equations, and the public `step()` API now defaults to that FIM path. A residual/Jacobian consistency bug was also fixed in `src/lib/ressim/src/pvt.rs`: without a PVT table the code now correctly reports `dBg/dp = 0` because the live residual path uses flat `Bg = 1`, instead of claiming gas compressibility that was not present in the actual residual. The next cutover slices are widening public-step parity on the FIM default path, fixing the newly exposed zero-flow well benchmark bug so the ignored Buckley/rate-control diagnostics can be promoted to normal regressions, removing the current explicit IMPES test opt-outs for Buckley/adaptive-substep/three-phase gas-injection expectations as they are ported, and then deleting the superseded IMPES pressure/transport stepping code.
  - Core cutover target: `step.rs` becomes Newton/timestep orchestration only, `pressure_eqn.rs` and `transport.rs` lose their production stepping roles, and a new coupled FIM residual/Jacobian path owns pressure, phase transport, flash, and well controls together.
  - Primary-variable target: natural-variable black-oil formulation with pressure + water saturation + (`sg` in saturated cells or `rs` in undersaturated cells), with explicit regime switching and local flash closure inside Newton rather than after transport.
  - Validation gate: existing Buckley-Leverett, PVT, well-control, gas-balance, boundedness, and reporting regressions must pass through the public step API on the FIM-only path before the migration is considered complete.
- [x] Refactor the Rust linear solver into a `solvers/` module tree and add faer sparse LU as the default backend.
  - Keep BiCGSTAB available behind the shared dispatcher so frontend solver selection can be added later without another core refactor.
  - Default-dispatch behavior now automatically falls back from faer sparse LU to BiCGSTAB when factorization fails or the direct-solve residual misses tolerance.
  - Validation status: `cargo fmt --manifest-path src/lib/ressim/Cargo.toml` completed and `cargo test --manifest-path src/lib/ressim/Cargo.toml` passed (`61 passed, 4 ignored`).
- [ ] Revisit the ignored Buckley-Leverett refined-grid regression as a potential solver/timestep issue, not just a slow-test classification problem.
  - Current status: the test is ignored in the default suite because it is expensive, but that does not rule out a remaining solver, timestep-control, or convergence issue on the refined path.
  - Follow-up when revisiting: run the refined cases individually with explicit timing/logging and inspect solver retries, accepted substep counts, and convergence behavior before deciding whether the slowness is expected.
- [ ] Continue moving embedded Rust test groups out of `lib.rs` into dedicated domain test modules.
  - First slice: Buckley-Leverett benchmark helpers and tests should live in their own test module rather than inside the crate root file.
  - Current status: Buckley-Leverett, PVT/property, and well-control/productivity tests now live in dedicated test modules.
  - Follow-up slices: split the remaining runtime/API/stability tests, geometry/reporting tests, and three-phase physics tests by domain so `lib.rs` stays a crate root instead of a mixed runtime/test container.
- [x] Split the remaining `step.rs` physics into dedicated pressure and transport modules so timestep orchestration is isolated from assembly/update details.
  - Extract pressure assembly, solve, and stability-factor calculation into `pressure_eqn.rs`.
  - Extract saturation/state-update helpers into `transport.rs`.
  - Keep `step.rs` focused on retry policy, timestep acceptance, and pressure-state sanity checks.
  - Validation status: `cargo fmt --manifest-path src/lib/ressim/Cargo.toml` completed and `cargo test --manifest-path src/lib/ressim/Cargo.toml -- --skip benchmark_buckley_leverett_refined_discretization_improves_alignment` passed (`57 passed, 3 ignored, 1 filtered`).
- [x] Audit the long-running Buckley-Leverett refined-grid regression and classify it appropriately in the Rust suite.
  - Audit result: the test is a four-run discretization-sensitivity sweep with two refined cases (`nx = 96`, `dt = 0.125`, `max_steps = 20000`), so it is better treated as opt-in slow coverage than as a default fast regression.
  - Action taken: `benchmark_buckley_leverett_refined_discretization_improves_alignment` is now `#[ignore]` with an explicit slow-test reason instead of being part of the default suite.
  - Validation status: `cargo test --manifest-path src/lib/ressim/Cargo.toml` passed (`57 passed, 4 ignored`).
- [x] Refactor Rust/WASM simulator module boundaries so `lib.rs` becomes a frontend/WASM facade rather than a mixed API + physics + test host.
  - Extract frontend-facing constructor/getter/setter/load-state code into `frontend.rs`.
  - Extract active well-control logic out of `step.rs` into `well_control.rs` and replace the stale duplicate module content from the incomplete earlier refactor.
  - Extract timestep output/reporting generation into `reporting.rs` so run outputs are emitted from one place.
  - Activate `mobility.rs` so `step.rs` no longer owns all mobility/capillary/gravity helpers.
  - Keep the current WASM/TypeScript API stable while reducing file size and improving future extensibility for FIM/compositional/well-schedule upgrades.
  - Validation status: Rust crate compiles cleanly and `cargo test -- --skip benchmark_buckley_leverett_refined_discretization_improves_alignment` passed (`57 passed, 3 ignored, 1 filtered`). The filtered Buckley-Leverett refined-grid regression remained long-running beyond a 3-minute wait and should be reviewed separately as a performance/coverage issue.
- [x] Apply continuous saturation weighting to the vertical-only simulation sweep path so `E_V` no longer depends on a hard cutoff.
- [x] Apply continuous saturation weighting to the combined simulation volumetric sweep path so hidden `E_vol` diagnostics do not depend on a hard cutoff.
- [x] Switch areal simulation sweep from binary swept-column counting to continuous cell weighting, while keeping vertical/combined sweep semantics unchanged.
- [x] Make the simulation sweep threshold use the actual initial water saturation and add regression tests for nonzero-`initialSaturation` and degenerate-Welge fallback behavior.
- [x] Revert Buckley-Leverett analytical cum-oil and oil-rate overlays; fix recovery normalization for completed-run references.
- [x] Extend Buckley-Leverett analytical chart wiring so cumulative-oil preview curves and completed-run oil-rate references use the same analytical source.
- [x] Fix Buckley-Leverett preview recovery normalization so nonzero `initialSaturation` uses OOIP rather than unit pore volume.
- [ ] Add black-oil comparative-solution validation (`SPE1` / `SPE3` style coverage, or the closest practical subset) and document the acceptance policy.
  - Review findings to preserve during follow-up work:
    - `SPE3` is not a good near-term comparison target for this repo: it is a fully compositional benchmark, while ResSim currently implements black-oil / three-phase flow but no EOS compositional model. It will not explain the current SPE1 black-oil mismatch directly.
    - Scalar-`cellDz` leakage remains in benchmark/chart normalization paths; `cellDzPerLayer` reaches the simulator core, but PV/OOIP and related review metrics still used the fallback scalar thickness in parts of the UI model.
    - SPE1 chart wiring violated the chart contract by mixing quantities in one panel. Reservoir pressure, producer WBHP, injector WBHP, control-limit fraction, GOR, and oil rate are now split into separate plots; keep future SPE1 overlays aligned to that one-quantity-per-panel rule with like-for-like simulation/reference series.
    - SPE1 now uses a dedicated `digitized-reference` analytical method instead of `none`, so published benchmark curves are modeled explicitly as a scenario reference mode rather than as an absence of reference behavior. Published-reference provenance is still mixed: average-pressure and GOR overlays use OPM-derived yearly samples, while oil-rate and WBHP overlays now come from the auditable brontosaurus `examples/spe1/expected_output/expected_summary.csv`. The repo still needs unified source provenance, variable mapping, unit conversions, and case-variant confirmation.
    - Per-layer completions currently mean repeated single-cell wells with shared controls, which is sufficient for current one-layer SPE1 wells but is not a general multi-completion-well model.
    - Validation and regression coverage for `cellDzPerLayer`, per-layer completions, and SPE1 reference-panel wiring remain incomplete above the Rust-core unit-test level.
  - Recommended implementation order:
    - Fix thickness normalization in benchmark/chart models first so SPE1 recovery/PV review uses the actual layered geometry.
    - Refactor SPE1 panels next so each quantity gets its own plot and reference curves use dashed styling consistently.
    - Reconstruct and check in auditable SPE1 reference provenance before tuning against the comparison curves.
    - Add focused regression tests for `cellDzPerLayer`, published-reference panel placement, and per-layer completion payload wiring.
    - If a second benchmark is added before compositional physics exists, prefer another black-oil comparative-solution target or additional auditable SPE1 summary vectors rather than `SPE3`.
  - [x] Per-layer cell thickness (dz) — Rust solver accepts `Vec<f64>` per layer instead of scalar.
  - [x] Per-layer initial gas saturation — Rust `setInitialGasSaturationPerLayer`.
  - [x] Worker wiring — TypeScript payload supports `cellDzPerLayer`, `initialSaturationPerLayer`, `initialGasSaturationPerLayer`.
  - [x] Per-layer well completions — worker supports `producerKLayers` / `injectorKLayers` for single-layer well completions.
  - [x] SPE1 scenario definition with published PVT, SCAL (Corey approximation), grid, and well data.
  - [x] Published reference data overlay — `publishedReferenceSeries` on Scenario type, wired through chart model with scatter markers.
  - [x] SPE1 chart layout (`spe1`) with pressure, GOR, oil rate, gas cut panels.
  - [x] Fix benchmark/chart thickness normalization so `cellDzPerLayer` drives PV/OOIP-based review metrics instead of falling back to scalar `cellDz`.
  - [x] Rework SPE1 chart layout to follow the chart convention: one quantity per panel, solid lines for simulation, dashed lines for reference, no mixed pressure/GOR panel.
  - [x] Honor SPE1 Case 1 `DRSDT = 0` semantics by disabling gas re-dissolution in the three-phase flash path.
  - [ ] Re-verify the SPE1 comparison source and metric mapping (Case 1 vs Case 2, average pressure vs field pressure, producing GOR vs summary-variable equivalent, units, and sampling cadence).
    - Current working mapping is `Avg Pressure vs time` and `producing GOR vs time` for Case 1 yearly samples. ResSim now carries explicit SPE1-style surface-rate targets plus BHP-limit diagnostics, but the control path is still a local PVT/state conversion rather than a full deck scheduler.
    - Exact OPM PVT plus SWOF/SGOF tables now reach the live simulator, and the earlier erratic GOR dips were traced to an implementation bug: the simulator was re-dissolving free gas even though SPE1 Case 1 is `DRSDT = 0`. Disabling gas re-dissolution removes that major non-physical oscillation source.
    - March 2026 PVTO audit correction: the SPE1 saturated lookup table tops out at the `5014.7 psia` saturated row (`345.73 bar`, `Rs = 1.618`), but the actual undersaturated continuation relevant to the initial `Rs = 1.27` branch is the separate `9014.7 psia` PVTO continuation row with `Bo = 1.579`. Matching the simulator's exponential undersaturated law `Bo(p)=Bo(pb)·exp[-c_o(p-pb)]` between `4014.7 psia` (`Bo = 1.695`) and `9014.7 psia` (`Bo = 1.579`) gives `c_o ≈ 2.06e-4 /bar`, so the earlier `8.53e-4 /bar` estimate from the saturated `345.73 bar` row was incorrect.
    - March 2026 PVTO implementation upgrade: the Rust `PvtTable` now preserves repeated-Rs PVTO continuations as explicit oil branches instead of collapsing all input rows onto one pressure-sorted saturated curve. Undersaturated oil lookups can now interpolate directly on tabulated PVTO continuation rows, with the scalar `c_o` retained only as a fallback when a branch has no continuation data or pressures exceed the tabulated continuation range. Focused Rust regressions now pin both the legacy hybrid SPE1 initial-state reconstruction and the new tabular Rs = 1.27 continuation interpolation.
    - Unit audit status: the FIELD-to-metric scalar conversions used in the local SPE1 scenario check out (`psia → bar`, `Mscf/STB → Sm³/Sm³`, `rb/Mscf → rm³/Sm³`, `STB/d → Sm³/d`, `Mscf/d → Sm³/d`, `psi⁻¹ → bar⁻¹`). Remaining comparison risk is more likely quantity-definition mismatch than unit conversion, especially `FPR`/summary-pressure semantics versus the simulator's current PV-weighted average reservoir pressure, and `FGOR`/`WGOR` semantics versus the simulator's produced-GOR diagnostic.
    - Auditable sparse SPE1 overlay series are now available for `FOPR` and `WBHP` via the brontosaurus representative CSV, but `FGOR` and `FPR` from that same file disagree materially with the denser OPM-derived local overlays. That conflict still needs source/metric reconciliation before those traces should be unified or replaced.
    - SPE1 injection plotting now has a dedicated rate panel wired to simulation `total_injection` plus a deck-target reference line derived from `WCONINJE RATE = 100000 Mscf/d` (`2,831,680 Sm3/d`). That reference is a control target, not a published historical summary vector, and should be replaced if an auditable SPE1 injection-rate series is checked in later.
    - Chart layout gotcha: adding a panel to `panelOrder` and `curveKeys` is not sufficient to render it. `ReferenceComparisonChart` resolves visibility as `override.visible ?? fallback.visible`, so new panels stay hidden unless either the scenario layout or the fallback explicitly sets `visible: true`.
    - Pressure-shape mismatch root cause identified: the IMPES total compressibility `c_t` was missing the dissolved-gas liberation term `So·(Bg/Bo)·dRs/dp` (Aziz & Settari Eq. 7.60). Below bubble point this term dominates c_t (~3.5× at SPE1 initial conditions), so the reservoir appeared far less compressible than it is, causing pressure to change too rapidly. Fixed by adding `get_c_o_effective()` which combines standard oil compressibility with the dissolved-gas term. Oil density was also corrected to `(ρ_o_sc + Rs·ρ_g_sc)/Bo` for proper gravity segregation.
    - Latest control/reporting audit: the earlier ORAT/RATE path converted surface targets to reservoir volumetric rates per completion from local PVT and fractional-flow state, then applied one-cell PI/BHP clamps. That was a major likely source of the post-breakthrough mismatch. The simulator now uses a shared-BHP group solution across producer completions and injector completions under rate control, so remaining SPE1 mismatch should be interpreted against that tighter well model rather than the older equal-split per-completion control. A smaller reporting bug was also found and fixed: gas material-balance diagnostics had been mixing free-gas reservoir volumes with dissolved-gas standard volumes, and gas injection surface totals were using `Bw` even for gas injectors.
    - Latest unit audit: the worker/store path preserves SPE1 surface-rate targets correctly, and the simulator reports oil, gas, and GOR at standard conditions consistently. A real transport bug was found and fixed in the Rust core: gas gravity had been using surface gas density instead of pressure-dependent reservoir gas density. The main remaining mismatch is still the simplified well model, not a broad STB/Sm3 vs reservoir-volume wiring error.
    - Undersaturated oil initialization fix: SPE1 Case 1's RSVD table specifies constant Rs = 226.20 Sm³/Sm³ (bubble point ≈ 277 bar), but the simulator was initializing Rs from the saturated PVT curve at initial pressure (331 bar → Rs ≈ 275). This caused oil to start saturated instead of undersaturated by ~54 bar, with cascading effects: immediate spurious gas liberation, too-high Bo (1.80 vs 1.68), too-low μo (0.46 vs 0.55), and incorrect effective compressibility. Fixed by adding undersaturation-aware PVT lookups (`interpolate_oil`, `bubble_point_pressure` in pvt.rs; `get_b_o_cell`, `get_mu_o_cell`, `get_rho_o_cell` in lib.rs) and a new `initialRs` parameter wired through the full TypeScript→Worker→Rust path. All `get_b_o` callers in step.rs now use the cell-aware `get_b_o_cell` which accounts for undersaturation.
    - Critical pore volume bug in pressure equation: `pore_volume_m3(i)` was called with the x-index `i` instead of the cell index `id` in the nested (i,j,k) pressure-solve loop (step.rs:483). For multi-layer grids with `cellDzPerLayer`, this caused all cells to use layer 0's dz, making the effective pore volume 60% of the true value for the SPE1 3-layer grid. Pressure responded 1.67× too fast. Previously masked because the incorrectly high c_t (saturated initialization) partially compensated for the too-small PV — two cancelling bugs. Fixed to `pore_volume_m3(id)`.
    - IMPES transport-pressure consistency fix: two structural inconsistencies in the IMPES scheme were causing erratic oscillations even at fine timesteps (0.25 days). (1) Well control decisions were resolved at old pressure for the pressure equation but at new pressure for saturation transport — if a well switches between rate-controlled and BHP-limited between the two evaluations, the saturation update uses a different volumetric rate than the pressure equation assumed. Fixed by pre-computing well controls once at old pressure and reusing them in both loops. BHP wells evaluate `PI*(p_new - BHP)` for the saturation rate, consistent with the implicit pressure treatment. (2) Upwind direction for phase fluxes in saturation transport was determined from new-pressure potentials, while the pressure equation used old-pressure potentials. When gas gravity causes flow direction to flip between old and new pressure, the transmissibilities differ, breaking volume conservation. Fixed by using old-pressure potentials for upwind direction selection (matching the pressure equation) while retaining new-pressure gradients for flux magnitude.
    - Three-phase flash / reporting consistency fix: the previous below-bubble-point update first reconstructed `Rs` on a pre-flash oil volume and then liberated gas by shrinking oil saturation afterward. That changed the oil inventory after `Rs` had already been computed, making total gas inventory timestep-sensitive. Fixed by replacing that sequence with a conservative local gas split driven by one total-gas inventory (free gas + dissolved gas) at the updated pressure. The timestep reporting path now also reuses the same resolved well-control state that transport used, and surface-rate-controlled injector reporting uses the control target in surface units rather than re-converting the reservoir transport rate with the updated `Bg`.
    - Pressure-spike root cause fix: the pressure matrix assembled in `step.rs` is not guaranteed symmetric once upwinded multiphase transmissibilities, gravity, and capillary terms are included, but it was still being solved with PCG. That can produce intermittent blow-ups and one-cell artifacts even when the timestep controller later keeps curves relatively close. Fixed by replacing the pressure solve with preconditioned BiCGSTAB and adding a pressure-state sanity gate that rejects obviously non-physical solved states before they enter rate history or 3D snapshots.
    - Remaining SPE1 kv/kh audit: the gravity term had still been using one-sided phase density at cell interfaces (`rho_i` only), which biases vertical phase potentials in compressible gas/oil flow and can exaggerate gravity override when `k_v/k_h = 1`. Updated the pressure and transport paths to use interface-averaged densities for water, oil, and gas. This should reduce the residual tendency for isotropic cases to over-break through gas relative to the restricted-`kz` sensitivities.
    - Remaining SPE1 energy-loss audit: free gas had still been transported in reservoir-volume units while dissolved gas and the gas material-balance diagnostic were tracked in standard-volume units. That unit mismatch can bleed gas support as pressure changes. Updated free-gas fluxes, well source terms, and the local gas split to carry free gas in standard conditions end-to-end before converting back to saturation at the updated pressure.
    - Terminal GOR reporting guard tightened: end-of-run `producing_gor` values became non-diagnostic once oil rate collapsed toward abandonment, so both the Rust runtime and the TypeScript comparison model now suppress GOR when total oil production is `<= 10 Sm3/d`. Focused Rust/TypeScript regressions, the full Rust suite, and a fresh WASM rebuild all passed with the stricter cutoff.
    - Remaining follow-up from this solver pass: timestep adaptation still estimates `max_well_rate_rel_change` from fully re-resolved old/new well controls (`well_rate_m3_day_for_pressures`) rather than the shared transport-control path. That can still overstate rate swings near control-limit transitions and should be aligned in a later iteration if timestep chatter persists.
    - Z-direction transmissibility bug fix: the geometric transmissibility for vertical connections between layers with different thickness was using `harmonic_mean(k1,k2) * A / avg_dist` instead of the correct half-cell harmonic mean `2·k1·k2·A / (k2·dz1 + k1·dz2)`. For SPE1's non-uniform layers (dz=6.1/9.1/15.2 m, kz=500/50/200 mD) this caused +16% overestimate for layer 0→1 and −15% underestimate for layer 1→2, distorting vertical gas migration and inter-layer pressure communication.
    - Gas inter-cell flux Bg consistency fix: gas flux between cells was converting from reservoir to standard conditions using `Bg(old_pressure)` while oil flux used `Bo(new_pressure)` and well terms used `Bg(new_pressure)`. Fixed gas flux to use `Bg(p_new)` consistently with oil and wells.
    - Gas injection reporting consistency fix: when the gas injector was rate-controlled, the reported SC injection used the target surface rate directly, but the saturation transport actually delivered `|q_reservoir|/Bg(p_new)` which differs when pressure changes. Fixed reporting to compute SC injection from the actual reservoir rate and local Bg, matching what transport delivers.
    - IMPES bubble-point compressibility smoothing: the hard cutoff in `get_c_o_effective` between undersaturated `c_o` (~2e-4) and saturated `c_o_eff` (~1.3e-3, ~6× larger) caused IMPES pressure overshoot at the bubble-point transition. Because IMPES uses OLD-timestep compressibility in the pressure equation, an undersaturated cell with too-small `c_t` allowed the pressure to drop well below the bubble point in a single step. The explicit saturation update then liberated excessive gas, triggering a positive feedback loop (more gas → lower oil mobility → more pressure drop → more gas). This was the root cause of: (a) GOR being much higher than Eclipse reference, (b) oil rate collapsing by 3× at breakthrough, (c) catastrophic gas flooding with low kv/kh. Fixed by adding quadratic blending in `get_c_o_effective`: when an undersaturated cell is within `max_pressure_change_per_step` of its bubble point, `c_o` smoothly transitions toward the saturated `c_o_eff`, allowing the pressure solver to "see" the upcoming stiffness increase and avoid overshooting.
    - Physics review of pressure equation: thorough audit of the IMPES pressure equation confirmed correctness of (a) total compressibility formulation `c_t = c_rock + So·c_o_eff + Sw·c_w + Sg·c_g`, (b) transmissibility assembly with upwinded phase mobilities, (c) explicit gravity and capillary RHS terms for all three phases, (d) well source terms for rate and BHP control modes, (e) Peaceman well PI with per-layer thickness, (f) surface-rate-to-reservoir-rate conversion for both producer (oil rate target → total rate via Bo/f_o) and injector (gas rate target → reservoir rate via Bg). X/Y transmissibility verified correct for same-layer connections (dz1==dz2 always). Remaining pre-breakthrough pressure lag vs Eclipse is likely inherent IMPES vs fully-implicit difference rather than a bug.
    - March 2026 review reproduction of the current base case still shows the remaining mismatch is concentrated at the producer control transition, not in broad unit conversion: around day 1095 the producer becomes fully BHP-limited, average pressure drops to ~335 bar vs ~404 bar reference, produced GOR jumps to ~3965 Sm3/Sm3 vs ~1329 reference, and oil rate falls to ~1228 Sm3/d while the reference remains near plateau. Refining to `Δt = 0.25 d` does not fix it; it worsens the post-breakthrough divergence slightly, and the finer-grid sensitivity also moved farther from the reference rather than converging toward it. Gas breakthrough timing stays comparatively close to the reference across timestep and grid sensitivities, but the post-breakthrough GOR rises too abruptly and to too high a level instead of following the slower ramp seen in the reference case. A focused Rust breakthrough probe now shows the onset of that jump happens before the producer becomes BHP-limited: by about 750–800 days the producer cell still has only `Sg ≈ 0.08–0.15`, yet local gas fractional flow has already climbed to `f_g ≈ 0.20–0.59`, and the rate-controlled ORAT path is increasing total reservoir withdrawal from about `5.3e3` to `1.26e4 rm3/d` to hold oil rate flat. The later BHP limit amplifies the mismatch, but the abrupt GOR ramp starts earlier from the coarse single-cell producer mobility / surface-rate conversion itself. The active mitigation now caches one shared same-layer sampled near-well producer state per accepted substep and reuses that exact state for producer ORAT control conversion, producer source partition, and producer reporting, instead of recomputing a different sampled state after the pressure update. That fixed a numerical/reporting regression where post-breakthrough producer oil rate could chatter because control was frozen on old pressure while reporting/source were re-evaluated on new pressure. A second late-time issue then appeared: accepted substeps could still cross the ORAT/BHP control boundary, so the last accepted rate-history point and the end-of-step resolved control could disagree around day ~1900–2000, producing excessive pressure decline and making the long-horizon decline look artificially cliff-like. The timestep controller now treats any ORAT/BHP mode crossing between old-pressure and new-pressure well-control resolutions as a forced cutback condition, so accepted substeps land on one side of the control boundary instead of stepping across it. The late-time SPE1 probe now keeps the producer finite through day 3000 rather than collapsing to zero near day 2000: after the BHP transition, oil rate declines gradually to about `1.93e3 Sm3/d` at day 2100 and `1.20e3 Sm3/d` at day 3000 for `Δt = 5 d`, and to about `2.39e3` at day 2100 and `0.77e3` at day 3000 for `Δt = 0.25 d`. The remaining mismatch is therefore still the coarse near-well producer model and control-transition semantics, not a raw surface/reservoir conversion bug.
    - March 2026 review of the local well state at the divergence point showed the producer cell is not catastrophically gas-filled (`Sg ≈ 0.24`, `So ≈ 0.64`) when the rate target fails. The remaining gap is therefore more consistent with near-well gas mobility / local ORAT control semantics than with a global units or pressure-equation bug.
    - March 2026 review found that the SPE1 producer/injector WBHP chart series are currently non-diagnostic: the comparison model reads `snapshot.wells[].bhp`, but the simulator snapshots only carry the static configured well `bhp` field, not the resolved flowing BHP from the rate-control solve. The WBHP comparison panels need explicit runtime reporting of resolved BHP before they can be used for tuning.
    - IMPES `c_o_effective` blending zone narrowed from `max_pressure_change_per_step` (50 bar for SPE1) to a fixed 5-bar margin near bubble point. The old 50-bar margin covered nearly the entire undersaturated range (277–327 bar vs initial 331 bar), inflating compressibility artificially and damping the pressure response to injection. `max_pressure_change_per_step` also reduced from 50 to 20 bar for tighter adaptive control. A stale-Rs bug in `apply_three_phase_deltas_to_cell` (transport.rs) was also fixed: `get_b_o_cell` was reading `self.rs[idx]` instead of the local `rs_old` parameter, causing incorrect Bo in the second step of the injector-cell two-step split.
    - IMPES well control oscillation fix: added 5% hysteresis margin to the BHP-limited threshold in `solve_well_bhp_for_pressures`. Previously the check was `target_rate >= max_achievable_rate - 1e-9` (effectively zero margin), so a well near the BHP limit would alternate between rate and BHP control every substep. With the margin, the well stays BHP-limited until the achievable rate exceeds the target by 5%, creating a dead band that prevents rapid mode switching.
    - March 2026 IMPES/FIM toggle fix: the SPE1 scenario was running the FIM solver path (`fimEnabled: true`) instead of IMPES. Since FIM is still under development and has known parity gaps (Buckley saturation-profile mismatch, rate-controlled flow differences, wasm timestep fragmentation), all four user-reported symptoms — less energy vs reference, injector cell oil buildup, gas flooding near producer, oil rebuilding in remote cells — were FIM solver artifacts. Fixed by setting `fimEnabled: false`. Also reverted `max_pressure_change_per_step` from 2 to 20 bar (2 bar was too aggressive for IMPES, causing excessive substep fragmentation). Comprehensive audit of the IMPES equation system (pressure matrix assembly, transport loop indices, neighbor connectivity, well connections, transmissibility formulas, gravity signs, gas inventory split, compressibility formulation) found no bugs — the IMPES path is correct.
    - Solver robustness fix from the March 2026 review: `step_internal()` was still accepting full SPE1 substeps even when the BiCGSTAB pressure solve failed to converge. On the current base case this happened on many 5-day steps without triggering adaptive substepping. The timestep loop now treats non-converged pressure solves exactly like other stability failures: shrink `dt`, retry, and only accept a substep once the pressure solve converges and the pressure state is physical. This removes accepted non-converged states from SPE1, but it does not materially change the remaining post-breakthrough mismatch, so that mismatch should still be investigated in the well/transport model rather than blamed on this solver issue.
    - Visual inspection trap fixed in March 2026: the 3D renderer had been drawing larger `k` higher in the scene even though the simulator/deck semantics use `k = 0` for the top layer, and it was still rendering a uniform scalar `cellDz` even when the simulator used `cellDzPerLayer`. The tooltip now shows both zero-based simulator indices and one-based deck indices, the rendered z-order now matches the simulator layer order, the 3D geometry now uses per-layer thicknesses, and live runtime now reads `cellDzPerLayer` from explicit reactive store state instead of only from completed-run params snapshots.
    - Low-`k_v/k_h` SPE1 injector-cell audit from March 2026: the top injector cell retaining oil is reproducible directly in the WASM simulator, so it is not a 3D-view artifact. Tightening `Δt` from 5 days to 1 day and 0.25 day does not remove it, and targeted Rust balance probing showed that at ~400 days the injector was still rate-controlled at about `2.83e6 Sm3/d` gas injection while nearly the entire injected gas volume was exported across the injector-cell faces within the same 5-day explicit transport step. Crude injector-face composition overrides made the result worse. A restrained explicit well-cell correction is now in place instead: active injector cells apply well-source inventory first and then intercell transport, following the same split-source idea used by MRST's explicit well-cell saturation update path. The retained low-`k_v/k_h` probe now gives injector `So ≈ 0.27`, `Sg ≈ 0.61` versus adjacent top-layer cells at `So ≈ 0.37`, `Sg ≈ 0.51`, which removes the non-physical oil-rich injector-cell artifact without destabilizing the probe or the focused gas-injection regression. Latest timestep sensitivity clarifies the remaining diagnosis: the injector-cell oil retention is still a local explicit source/transport splitting error that shrinks substantially as `Δt` is reduced (`So ≈ 0.396` at 5 d, `0.299` at 2.5 d, `0.151` at 1.25 d, `0.026` at 0.25 d), so the cell-flooding pathology is not the main cause of the SPE1 GOR mismatch. The remaining follow-up is to re-check the full SPE1 comparison curves against the current user-observed symptoms: finer timesteps materially improve local fluid distribution near the injector but do not fix the high post-breakthrough GOR, gas breakthrough timing remains approximately correct across timestep and grid sensitivities, the simulation GOR rises too sharply immediately after breakthrough instead of ramping gradually like the reference, and finer grid still moves the overall comparison farther from reference rather than converging toward it.
  - [ ] Add regression tests for SPE1 scenario wiring, published-reference panel placement, and the `cellDzPerLayer` / per-layer completion payload path.
    - Current coverage now includes `cellDzPerLayer` normalization, the dedicated SPE1 GOR panel/style contract, pre-run published-reference visibility, exact SWOF/SGOF payload cloning, store wiring that preserves scenario-supplied PVT/SCAL tables, scenario/create-payload wiring for `gasRedissolutionEnabled` and `initialRs`, and a worker-parity SPE1 runtime regression that verifies `producerKLayers` / `injectorKLayers` instantiate only the requested layer completions.
    - Benchmark/runtime gap from the March 2026 review: `benchmarkPresetRuntime.test.ts` still does not exercise the exact SPE1 runtime path. It uses scalar `cellDz`, omits PVT/SCAL/initial-Rs setup, and adds completions across every layer instead of the scenario's single-layer wells, so it cannot catch the current SPE1 mismatch.
  - [ ] Tune SPE1 rate targets and validate against Eclipse reference (qualitative match expected; exact match requires tabular SCAL).
    - Surface-rate targets and explicit BHP-limit diagnostics are now in place. Exact OPM PVT plus SWOF/SGOF inputs are also in place, SPE1 Case 1 now honors `DRSDT = 0`, and the fine-grid `grid_20` sensitivity carries tighter numerics to reduce stability complaints. Remaining mismatch should be explained by simulator behavior, not by benchmark-specific curve fitting.
  - [x] Add tabular SCAL support to Rust solver and worker/store payload path so SPE1 can use exact OPM SWOF/SGOF tables.
- [ ] Define the exit criteria for three-phase `experimental` status and add acceptance tests for gas injection and gas-drive scenarios.
- [ ] Reconcile all three-phase docs with the implemented state: corrected gas-oil capillary sign, `s_org`, explicit gas material-balance reporting, and remaining oil-phase diagnostic limits.
- [ ] Add regression tests for comparison-model preview mode, depletion per-variant analytical overlays, and color-index stability.
- [ ] Add a guard test for the duplicated undersaturated `c_o = 1e-5 /bar` assumption shared by the black-oil PVT generator and material-balance helper.
- [ ] Fix chart x-axis endpoint generation for cumulative/time modes: prepend zero anchors where appropriate and snap shared x-range/ticks to round values so Chart.js does not expose floating residues like `0.006` or `70.00000000006`.

## Next

- [ ] Enforce analytical-method semantics at the scenario type level so sweep scenarios cannot accidentally inherit Buckley-Leverett-style primary curves or disclosure rules.
- [ ] Generalize the `sweep_combined` analytical-method toggle into a reusable sweep-method framework.
- [ ] Collapse the remaining legacy benchmark layer into the scenario system where practical.
- [ ] Extract a typed output-selection view model from `App.svelte` for charts, 3D output, and analytical helpers.
- [ ] Decide whether `SwProfileChart` should be restored as a maintained output or removed completely.
- [ ] Document `sweep_areal` explicitly as a quarter-five-spot style interpretation with no-flow outer boundaries.

## Later

- [ ] Add per-scenario parameter overrides without forcing a switch into full custom mode.
- [ ] Add multi-case 3D inspection and synchronized case selection across outputs.
- [ ] Add scenario and results export/import.
- [ ] Add gas-cap scenarios on top of the validated black-oil base.
- [ ] Add Warren-Root style vertical sweep blending for finite vertical communication.
- [ ] Add aquifer models, schedules, non-uniform grids, and horizontal wells only after the current validation backlog is closed.

## Reference Notes To Keep

- `sweep_ladder` intentionally leaves analytical overlays shared even though the patched viscosity would change the analytical result. That is a teaching choice, not a bug.
- In black-oil three-phase mode, `get_c_o_effective()` replaces `get_c_o()` for the IMPES accumulation term. It includes the dissolved-gas compressibility `(Bg/Bo)·dRs/dp` which is the dominant compressibility contributor below bubble point. The original `get_c_o()` (with positive-value clamping) is still used for two-phase mode where dissolved-gas effects are not modeled.
- Water and gas cumulative material-balance errors are reported explicitly in three-phase mode. Oil is still the residual phase in diagnostics.

### SPE1 FIM Stall — Resolved
The SPE1 Gas Injection case stalled during Newton iterations early in the FIM run. Two root causes were identified and fixed:
1. **Gas injector mobility was zero** (`wells.rs`): 3-phase injectors used only the injected phase's mobility (`krg=0` at `Sg=0`). Fixed: all injectors now use total mobility (consistent with 2-phase path). Injected component tracked separately via `perforation_component_rates_sc_day`.
2. **Fischer-Burmeister slacks were unscaled** (`wells.rs`, `assembly.rs`, `scaling.rs`): `bhp_slack` ~O(100 bar) vs `rate_slack` ~O(10^6 Sm³/d) caused `∂FB/∂rate → 0`, killing Newton sensitivity to rate changes. Fixed: slacks normalized by `bhp_limit` and `target_rate` before FB evaluation; Jacobian chain rule updated; equation scaling set to 1.0.
- Regression test `spe1_fim_first_steps_converge_without_stall` added. All 20 FIM assembly Jacobian verification tests pass.

### FIM Performance Optimization — Applied
SPE1 FIM was extremely slow on WASM. Combined 3.3× native speedup (13.5× from original) through:
1. **Lightweight `respects_basic_bounds()`** (`state.rs`): Replaced full PVT flash per cell with arithmetic bounds check. Was called per damping attempt (up to 10× per Newton iteration).
2. **Removed redundant `enforce_control_bounds()`** (`state.rs`): Was called twice in `apply_newton_update()`, each rebuilding well topology. Now called once.
3. **Early Newton termination** (`newton.rs`): If residual stagnates (≥95% of previous) for 3+ iterations, bail early with `cutback_factor=0.25` instead of wasting remaining iterations.
4. **Assembly caching** (`assembly.rs`): Pre-compute `FimCellDerived` and `LocalFluxCellSensitivity` once per Newton iteration. Previously `derive_cell()` was called 4-6× per interface (residual + Jacobian). For 10×10×3 grid: ~4000 calls reduced to ~300.
5. **Unified min_damping** (`newton.rs`): Removed WASM-specific `1/1024` (10 halvings); both paths now use `1/64` (6 halvings).
6. **Appleyard chop** (`newton.rs`): Per-variable Newton damping limits (ΔP ≤ 500 bar, ΔSw ≤ 0.5, ΔRs ≤ 100%). Prevents gross overshoots without strangling convergence. Generous limits — safety net, not convergence control.
7. **Timestep control overhaul** (`step.rs`): Previous scheme used cutback_factor from Newton (0.225×) and slow 1.5× growth, causing catastrophic timestep collapse (1108 substeps/30 days). Fixed: simple 0.5× halving, 2.0× growth, state built once outside retry loop. Result: 48 substeps/30 days.
- Full SPE1 run (3600 days): ~6 min native release, ~31k substeps. ECLIPSE runs this at dt=30 with internal refinement.
8. **GMRES: Givens residual estimate** (`gmres_block_jacobi.rs`): Inner loop was computing a full matrix-vector product + back-substitution + basis combination at every iteration just for a convergence check. Replaced with `|rotated_rhs[inner+1]|` — the Givens rotation already gives the residual estimate for free. Solution is only constructed when convergence is detected or at restart boundary.
9. **CPR enabled on WASM** (`linear/mod.rs`): WASM was using `GmresIlu0` (no pressure correction) while native used `FgmresCpr`. For pressure-dominated problems (waterflood), missing CPR means many more GMRES iterations. No platform reason to disable it.
10. **Well topology cached in Newton loop** (`newton.rs`, `state.rs`, `assembly.rs`): `build_well_topology()` was rebuilt per Newton iteration (in assembly) and per damping attempt (in `enforce_control_bounds`). Now built once in `run_fim_timestep` and passed through.
11. **Regularized Fischer-Burmeister** (`wells.rs`): The NCP function `sqrt(a²+b²) - a - b` has a non-differentiable point at the origin — exactly the well control switching point where both BHP slack and rate slack approach zero. This creates a Jacobian discontinuity that kills Newton convergence at water breakthrough (producer switching between rate-controlled and BHP-limited). Fixed with `sqrt(a²+b²+2ε²) - a - b` (ε=1e-6), making the function C¹ everywhere.
12. **Relaxed Newton convergence** (`newton.rs`): Accept convergence on residual alone when residual is very tight (≤1% of tolerance), even if update norm is still above tolerance. At well control switching, the update can oscillate while the system is already well-balanced — refusing convergence there forces unnecessary timestep cuts.

### FIM Performance — Future Improvements
The remaining gap to ECLIPSE (dt=30 converges, we average ~0.1 day substeps) is in Newton convergence radius. Ranked by expected impact:

1. ~~**Variable switching with hysteresis (Sg ↔ Rs)**~~: **UPDATED.** Newton loop still keeps the regime map frozen within a solve via `apply_newton_update_frozen()`, but post-convergence classification now switches undersaturated cells immediately once `Rs` materially exceeds `Rs_sat` and flashes the excess gas conservatively instead of using the old 1.01× overshoot band. Near-zero saturated cells are also re-flashed before collapsing back to undersaturated so regime cleanup does not silently drop gas inventory.

2. **Newton initial guess via extrapolation**: Currently each substep starts from the previous converged state. Linear extrapolation from last 2 converged states would give Newton a better starting point, especially when the solution is evolving smoothly between substeps. Low-risk but moderate impact — mostly helps with smooth evolution, less with the hard phase-front steps.

3. **Residual-based line search (cheap)**: Evaluate only the residual (not full Jacobian) at candidate states during damping to accept the largest step that actually reduces the residual. The stashed attempt computed the FULL assembly at each damping level (catastrophically expensive). Correct implementation: expose a residual-only assembly path and use Armijo-type acceptance. Would save wasted Newton iterations on bad updates.

4. **Appleyard chop tuning / DPMAXL-style timestep control**: ECLIPSE uses DPMAXL/DSMAX to control timestep sizes based on converged variable changes across steps (not within Newton). Our current timestep control is purely convergence-based (did Newton converge y/n). Adding a max-change-per-step criterion would prevent the timestep from growing into regimes where Newton barely converges but variable changes are unreasonably large.

5. **CPR preconditioning (complete the FGMRES-CPR path)**: The current CPR path is incomplete — falls back to block-Jacobi + ILU0 for the pressure stage. True CPR with AMG or ILU on the pressure-reduced system would give mesh-independent linear convergence, enabling larger grids.

6. **Jacobian reuse / lagged Jacobian**: Reuse the Jacobian from the previous iteration when state hasn't changed much. Trades convergence rate for assembly cost. Most effective when assembly dominates solve time (smaller grids, WASM).

7. **IMPES predictor for Newton initial guess**: Run one cheap IMPES-style pressure solve + explicit saturation update as the starting point for Newton. "Predictor-corrector" approach used in some simulators.

8. **Pseudo-transient continuation**: Add a temporary diagonal mass term early in hard Newton solves and relax it as residual drops. Widens convergence basin without changing the converged solution.
