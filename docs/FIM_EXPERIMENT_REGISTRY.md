# FIM Experiment Registry

This is the fast pre-flight index for FIM convergence work. Before proposing
or implementing a FIM solver change, search this file for the mechanism, files,
target case, and failure family. If an equivalent experiment is already listed,
do not repeat it unless the `Retry only if` condition is satisfied.

Use this file for short, searchable verdicts. Keep detailed traces, tables, and
reasoning in the linked source documents.

## Registry Rules

- One row per attempted lever or diagnostic branch.
- `Verdict` values:
  - `PROMOTED` - kept in the solver or workflow.
  - `REVERTED` - tried and removed; do not repeat casually.
  - `REFUTED` - measured as a no-op or wrong mechanism.
  - `INCONCLUSIVE` - the oracle was invalid/incomplete or a masking defect prevented the stated
    hypothesis from being measured; repair the gate before selecting another mechanism.
  - `DIAGNOSTIC` - instrumentation or read-only result.
  - `OPEN` - promising but not yet attempted or closed.
- `REFUTED` requires a valid, hypothesis-relevant oracle. Missing/backend-specific diagnostics,
  incomparable reduced/full-system norms, or an incomplete coupled mechanism must be recorded as
  `INCONCLUSIVE`, even if the implementation was reverted.
- Record exact commands and baseline numbers in the source document, not in
  this index.
- When a future experiment supersedes a row, update the old row instead of
  adding a conflicting duplicate.

## High-Risk Repeats

| ID | Area | Hypothesis / Lever | Main Files | Main Cases | Result | Verdict | Retry only if | Source |
|---|---|---|---|---|---|---|---|---|
| FIM-AD-001 | Assembly | Switch live assembler from legacy hand derivatives to AD | `fim/assembly_ad.rs`, `fim/properties.rs`, `relperm.rs`, `fim/wells_ad.rs` | FIM control matrix, heavy water, gas 6-step, full suite | Found/fixed two-phase structural singularity plus Stone2 and connection-rate clamp kinks; gas shelf improved, heavy water initially mixed | PROMOTED | Future AD/legacy divergence touches assembly/properties/wells/relperm | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` "Phase 5 AD-assembler cutover" |
| FIM-AD-002 | Assembly | Delete legacy assembler after AD cutover | `fim/assembly.rs`, `pvt.rs`, `mobility.rs`, `wells.rs` | Production build, full suite, wasm size, shortlist | Kept legacy assembler as test-only parity oracle; gated dead-code helpers with `#[cfg(test)]` | PROMOTED | Only if a replacement parity oracle exists | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` "Phase 6 completed" |
| FIM-NEWTON-001 | Newton | OPM-style oscillation detection and relaxation scalar | `fim/newton.rs` | Full control matrix, heavy water | Heavy water improved `34 -> 31` substeps, controls unchanged | PROMOTED | Tune constants only with fresh control-matrix evidence | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` "Phase 7" |
| FIM-NEWTON-002 | Newton | Remove `nonlinear_history_stabilization_decision` after OPM relaxation | `fim/newton.rs` | Heavy water step trace | Redundancy hypothesis false; history cap tighter in 29/29 firings | REFUTED | OPM relaxation is retuned and shown tighter at those sites | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` "Sub-phase 7.3" |
| FIM-NEWTON-003 | Newton | Remove producer-hotspot stagnation bailout | `fim/newton.rs` | Heavy water, water-rate 20x20x3, gas controls | Zero fires across shortlist; mechanism removed | PROMOTED | New topology proves the removed mechanism is needed | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` "Sub-phase 7.4" |
| FIM-NEWTON-004 | Newton | Loosen or remove residual-stagnation bailout / above-tolerance stagnation acceptance | `fim/newton.rs` | Heavy water, medium water | Bailout still load-bearing; prior widening attempts regressed or no-oped. Follow-up audit (Bundle Y Y1i) found `would_widen` is trace-only: the live Legacy block activates at `stagnation_count >= 3` and its acceptance band permits residuals up to `10×` tolerance regardless of the trend value. | REVERTED | Do not retry while Y2b bound/update parity is open. Retry only after a demonstrated state/Jacobian fix is promoted and a clean exact oracle still plateaus above tolerance; OPM's lower iteration count alone never authorizes broader acceptance. | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` "Sub-phase 7.5"; `docs/FIM_OPM_PARITY_PLAN.md` §12 |
| FIM-DIAG-001 | Diagnostics | Add nonlinear state detail at linear failure sites | `fim/newton.rs` | Heavy water, 23x23x1 | Pure instrumentation; controls bit-identical; exposed well-source-dominated cell rows | PROMOTED | N/A | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` "Phase 8" |
| FIM-LINEAR-001 | Linear/CPR | Blanket per-row infinity-norm scaling before iterative CPR/ILU0 | `fim/linear/gmres_block_jacobi.rs` | Heavy water and full control matrix | Heavy water regressed `31 -> 241` substeps; controls mostly unchanged | REVERTED | Scaling is equation-family-aware or CPR-pressure-only, not blanket full-system row scaling | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` "Hypothesis C attempt" |
| FIM-LINEAR-002 | Linear | Row/column scaling probe (`D_r J D_c`) | `fim/linear/gmres_block_jacobi.rs` | 4-case shortlist | No clean win; medium 6-step stalled; heavy/gas regressions | REFUTED | Preconditioner chain changes first, especially pressure weighting | `docs/FIM_LINEAR_SOLVER_AUDIT.md` "Finding 2" |
| FIM-LINEAR-003 | Linear | Intra-rung Jacobian / sparse-LU factorization reuse | `fim/newton.rs`, `fim/linear/mod.rs`, `fim/linear/sparse_lu_debug.rs` | Medium water, heavy water, gas shortlist | Strict gate added overhead; permissive gate diverged trajectory | REVERTED | Bypass architecture changes so hot iterations solve same-shape systems | `docs/FIM_JACOBIAN_REUSE_INVESTIGATION.md` |
| FIM-LINEAR-004 | Linear/Newton | Post-fail short-circuit after iterative CPR failure | `fim/newton.rs` | 4-case shortlist | Reduced wasted repeated CPR attempts; later work showed it is cost-saving, not substep-closing | PROMOTED | Revisit only if CPR backend behavior changes materially | `docs/FIM_BYPASS_AUDIT.md` "Lever 3 Stage 2" |
| FIM-LINEAR-005 | Linear/CPR | CPR pressure restriction should not be unconditional water row | `fim/linear/gmres_block_jacobi.rs:2047` | Heavy water 54 captured systems, bounded 23x23x1 13 captured systems (both captured under **Legacy** nonlinear flavor — see correction below) | Tested as full solves in the offline lab: `row0-schur` converged 0/54 and 0/13; `sum-rows` and `quasi-impes` both converged ~92-93% on both corpora (heavy 50/54, bounded 12/13); `dominant-diag-row` strong on bounded (12/13) but weaker on heavy (39/54); `diag-balanced-sum`/`local-schur-balanced` no better than row0-schur | **PROMOTED — status corrected 2026-07-12 (Bundle Y Y1), row was stale** | `solve()`'s hardcoded restriction has been `CprPressureRestrictionKind::QuasiImpes` since commit `77ec900e` (2026-07-05, Step 9.3) — this row was never updated to reflect that when Phase 10/11 built on top of it. **New finding, not yet closed**: the offline lab that produced the numbers above can only ever capture **Legacy**-flavor failures — the capture hook (`fim/newton.rs` ~line 3587) sits inside `if !opm_aligned { ... }`; nothing analogous exists on the `OpmAligned` no-fallback path (~line 3700). Bundle Y's target is `OpmAligned`, so this promotion's evidence base has never been validated against the flavor it's actually running under in production. A fresh `OpmAligned` capture attempt (Bundle Y Y1, 2026-07-12) on the gas-rate case's 337 linear-bad retries captured **zero** systems, confirming the gap empirically. Separately, the lab's comparison harness (`solve_with_restriction_kind`) calls `gmres_block_jacobi` directly, bypassing `well_schur::solve_with_well_elimination` (`FIM-LINEAR-010`, also live) — so even a Legacy-flavor rerun today would not exercise the actual production code path | Add an `OpmAligned`-side capture hook (mirroring `fim/newton.rs`'s existing `!opm_aligned` one) before any further restriction-variant work is trusted as evidence for the flavor Bundle Y targets; re-run the offline lab through `solve_linearized_system` (not `gmres_block_jacobi::solve` directly) so well-Schur elimination is included | `docs/FIM_CONVERGENCE_WORKLOG.md` "Restriction-variant comparison"; `docs/FIM_LINEAR_SOLVER_AUDIT.md` "Finding 3"; `docs/FIM_OPM_PARITY_PLAN.md` §7 (Y1 findings) |
| FIM-LINEAR-006 | Linear/CPR | Stronger coarse solver / AMG path | `fim/linear/gmres_block_jacobi.rs` | OPM ablations, over-threshold probes | OPM CPRW gives large linear-iteration savings; ResSim still uses dense <=512 else BiCGSTAB+ILU0. Exact gas first-rung direct/live equivalence refutes coarse quality as the current convergence cause. | OPEN — DEFERRED SCALE-UP | Re-open only on a larger case where measured coarse-solve error changes the Newton direction or convergence; not before Y2 bound/update parity | `docs/FIM_LINEAR_SOLVER_AUDIT.md`; `docs/OPM_FLOW_MINIMAL_MAPPING.md`; `docs/FIM_OPM_CONVERGENCE_EXECUTION_PLAN.md` |
| FIM-LINEAR-007 | Linear/CPR | Build offline capture/replay solver lab; test whether plain ILU0 (no CPR) beats current CPR on real failing systems | `fim/linear/capture.rs`, `fim/linear/solver_lab.rs`, `fim/newton.rs`, `fim/timestep.rs` | Heavy water 54 captured systems, bounded 23x23x1 13 captured systems | Both validity gates passed both corpora (0 sparse-LU failures; 54/54 and 13/13 live-failure reproduction offline). Heavy case: plain GmresIlu0 beat current FgmresCpr by 1-4 orders of magnitude on every system (CPR pressure-correction actively harmful there). Bounded case: mixed, CPR sometimes much better/worse, no uniform direction | DIAGNOSTIC | Test salvaged `CprPressureRestrictionKind` variants + OPM quasi-IMPES weighting as full-solve lab variants before any live-solver change | `docs/FIM_CONVERGENCE_WORKLOG.md` "Phase 9 (revised 2026-07-04)" |
| FIM-TIME-001 | Timestep | Cross-outer-step hard dt-cap carryover shared across water/gas | `fim/timestep.rs` | Heavy water, gas 6-step | Helped one gas shelf but regressed water guard; reverted | REVERTED | Family-specific policy with water guard evidence | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` "Water shelf" / "Gas shelf" |
| FIM-TIME-002 | Timestep | Gas-only outer-step first-trial carryover with fixed clean-step budget | `fim/timestep.rs` | Gas-rate 10x10x3 6-step, control matrix | Stabilized shipped gas replay after startup; bounded controls unchanged | PROMOTED | Change persistence only with full sequential gas replay and water controls | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` 2026-04-12 gas carryover |
| FIM-TIME-003 | Timestep | Accepted-site-aware carryover persistence | `fim/timestep.rs`, `fim/newton.rs` | Gas-rate 10x10x3 6-step | Narrower site-aware variants failed to hold full replay | REVERTED | New evidence beats fixed 3-clean-step budget | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` 2026-04-12 comparison branch |
| FIM-TIME-004 | Timestep | Post-cooldown hotspot regrowth caps | `fim/timestep.rs` | 20x20x3 water, controls | Added fragmentation without reducing retries | REVERTED | New front-local mechanism identifies why regrowth fails | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` 2026-04-11 water trace follow-up |
| FIM-TIME-005 | Timestep | Let exact no-op accepts decay hotspot memory earlier | `fim/timestep.rs` | Heavy water | Reopened `water@1020` ladder; severe retry/runtime regression | REVERTED | New release policy prevents same hotspot refail after memory clears | `docs/FIM_CONVERGENCE_ARCHIVE_2026-04-08_to_2026-07-03.md` 2026-04-11 heavy guard |
| FIM-TRIAL-001 | Newton initial guess | Global/per-cell Newton extrapolation | `fim/timestep.rs`, `fim/newton.rs` | Heavy water, SPE1, shortlist | Three rounds failed: replay regression, gas regression, SPE1 collapse | REVERTED | Fundamentally different cell-selection anchor than Step-0 top-risk cells | `docs/FIM_SLICE_A_EXTRAPOLATION.md`; `docs/FIM_CONVERGENCE_IMPROVEMENTS.md` |
| FIM-TRIAL-002 | Replay/acceptance | Dt-aware replay acceptance tolerance | `fim/timestep.rs` | 6-case shortlist | Measured no-op; gate requires exactly zero update/diff | REFUTED | Replay gate precondition changes, but that overlaps failed extrapolation path | `docs/FIM_CONVERGENCE_IMPROVEMENTS.md` "Direction B attempt" |
| FIM-TRIAL-003 | Timestep | Water cross-outer-step first-trial carryover | `fim/timestep.rs` | Medium-water 6-step, shortlist | Fired but behavior bit-identical; boundary cold-start was not cost source | REVERTED | Evidence shows boundary first trial, not in-step ladder, is dominant | `docs/FIM_CONVERGENCE_IMPROVEMENTS.md` "Direction E attempt" |
| FIM-DAMP-001 | Newton damping | Ignore zero-move effective-floor iterations in stagnation counter | `fim/newton.rs` | 4-case shortlist, OPM comparison | Medium-water substeps improved strongly; heavy case moved closer to fine-dt physical reference | PROMOTED | Revisit only with fine-dt/OPM reference, not raw substep count alone | `docs/FIM_LINEAR_SOLVER_AUDIT.md` "Fix A1 Stage 2" |
| FIM-DAMP-002 | Newton damping | Remove Wang-Tchelepi fractional-flow inflection chop entirely | `fim/newton.rs` | Case 3 correctness discriminator | FOPT/physics degraded; full removal refuted | REVERTED | New correctness reference shows no basin-jump risk | `docs/FIM_LINEAR_SOLVER_AUDIT.md` "Fix A3 Stage 2" |
| FIM-DAMP-003 | Newton damping | Widen fractional-flow inflection chop threshold (`k=1.2`) | `fim/newton.rs` | 4-case shortlist, fine-dt references | Net lin_ms improved, correctness preserved vs fine-dt/OPM references. **Superseded 2026-07-05 by `FIM-DAMP-004`** — a fresh k-sweep under the Phase 10/11 linear-solver bundle found `k=1.2` is no longer the sweet spot (in fact a locally bad value); `k=1.25` is now live | PROMOTED (historical; `k` value itself superseded) | See `FIM-DAMP-004` — do not re-derive `k` without a fresh sweep if the linear solver changes again | `docs/FIM_CHOP_WIDEN_EXPERIMENT.md` |
| FIM-LINEAR-008 | Linear/CPR | Adopt OPM's `cprw` recipe as one bundle: loosened linear tolerance (`1e-7`→`5e-3` relative, exact translation since `r_0=rhs`), reduced iteration budget (`150`→`20`), block-ILU0 fine smoother (matching `Dune::BCRSMatrix<MatrixBlock>`), all with `quasi-impes` (already live) | `fim/linear/mod.rs`, `fim/linear/gmres_block_jacobi.rs` | Heavy water 54 captured systems, bounded 23x23x1 13 captured systems, full control matrix + locked smoke | Offline lab: decisive win, 100% convergence on both corpora (up from 50/54, 12/13), 5-6x fewer iterations. Live reconciliation and the accepted fine-dt reference were completed in later checkpoints (`FIM-DAMP-004`; Task #38). The stack remains live, with known trajectory sensitivity documented. | PROMOTED | Re-open only with a physics-accuracy regression on a clean current baseline, not to tune substep counts | `docs/FIM_CONVERGENCE_WORKLOG.md` "Phase 10 (2026-07-04)", "Task #38" |
| FIM-NEWTON-005 | Newton | Post-loop-only near-converged-final-state acceptance (accept the max-iteration Newton result when residual is within 10x tolerance and mb/update are already comfortably converged) — narrower in scope than the previously-reverted mid-loop stagnation-widening (`FIM-NEWTON-004`; that one gave up early with budget remaining, this one only fires after full budget spent | `fim/newton.rs` | Heavy water 12x12x3/dt=1 under the `FIM-LINEAR-008` bundle | Live run did not finish in over 8 minutes (vs `152.5s` bundle-only, `~26-78s` earlier baselines) — killed. Accepting the marginally under-converged `perf@1299` state carries error forward into subsequent substeps' initial conditions and compounds rather than resolving | REFUTED | A fundamentally different well/perforation convergence architecture (e.g. dedicated well-operator tolerance, not a bounded final-state acceptance) — not another acceptance-factor tune | `docs/FIM_CONVERGENCE_WORKLOG.md` "Step 10.4 (reopened)" |
| FIM-LINEAR-009 | Linear/CPR | Per-family/per-block linear stopping criterion (`EquationScaling::family_peaks`/`within_relative_reduction`, threaded as opt-in `equation_scaling` through `gmres_block_jacobi::solve_with_cpr_fine_smoother`'s convergence checks) — reuses Newton's existing per-equation-family scaling, requires every family (not just the whole-system norm) to hit its own relative-reduction target | `fim/scaling.rs`, `fim/linear/gmres_block_jacobi.rs`, `fim/linear/mod.rs`, `fim/linear/capture.rs` (v2 format) | Heavy water 35 final-iteration captured systems, bounded 23x23x1 3 captured systems (recaptured against the live `FIM-LINEAR-008` bundle) | Heavy corpus: only 1/35 systems shows real per-family overshoot (12.14x); the other 34 already satisfy their own family target under the existing global criterion. Enforcing it anyway costs ~25% more mean linear iterations for one fewer converged system (35/35 → 34/35). Bounded corpus: overshoot ratios in the billions are a division-by-near-zero artifact (tiny raw RHS at a near-converged iteration), not a real signal — both configs fail identically (0/3) regardless | REFUTED as primary cause | New offline evidence that a specific family is genuinely, non-trivially left unresolved across a broad captured sample — not this narrow 1/35 result | `docs/FIM_CONVERGENCE_WORKLOG.md` "Step 10.1 follow-up" |
| FIM-LINEAR-010 | Linear architecture | Schur-eliminate well-BHP + perforation-rate rows from the linear system every Newton iteration (matching OPM's `StandardWellEquations`), recovering them exactly after the reduced reservoir-only solve, rather than solving wells as ordinary global unknowns | `fim/linear/well_schur.rs` (new), `fim/linear/mod.rs` (`eliminate_wells` option + dispatch), `fim/linear/gmres_block_jacobi.rs` (3 helpers made `pub(super)` for reuse) | Synthetic exact-match unit test, 35 heavy + 3 bounded captured systems (offline), full control matrix + heavy case (live) | Correctness proven exact (`1e-9` match vs direct solve). Offline: decisive win (34/35→35/35 converged, mean linear iters 3.9→1.1). Live: heavy case regressed alone (`59→160` substeps) — but the `perf@1299` oscillation trace is byte-for-byte unchanged before/after, proving elimination doesn't touch the nonlinear oscillation (it's an exact reformulation, same Newton direction either way). Combined with `FIM-NEWTON-006`: heavy case at `62` substeps, non-target control matrix bit-identical, locked smoke 3/3 | PROMOTED (kept live) | Re-evaluate only after `water@387`'s "invalid bounded Appleyard candidate" is understood — this component's own value (correctness + offline efficiency + OPM-architecture consistency) stands independent of that | `docs/FIM_CONVERGENCE_WORKLOG.md` "Phase 11 (2026-07-04)" |
| FIM-NEWTON-006 | Newton | Widen `PerFamilyNorms`/`detect_oscillation` (Phase 7's OPM-ported oscillation detector) to include `well_constraint`/`perforation_flow`, previously excluded pending evidence | `fim/newton.rs` | Heavy water 12x12x3/dt=1, full control matrix, locked smoke | Measured `perf@1299`'s scaled residual alternating `d1≈0, d2≈0.6` — textbook match for the detector's own test, invisible to it purely due to family-scope exclusion. Live: heavy case `160→62` substeps (combined with `FIM-LINEAR-010`), `40→20` retries, `hotspot_newton_caps` `58→16`, `retry_dom` shifted from `perf@1299` to a different site/mechanism entirely (`water@387`, "invalid bounded Appleyard candidate" — unrelated). Control matrix bit-identical, locked smoke 3/3 | PROMOTED | Re-open only if a future family shows the same detector-blind-spot pattern | `docs/FIM_CONVERGENCE_WORKLOG.md` "Phase 11 (2026-07-04)" |
| FIM-NEWTON-007 | Newton | Fix the fw-inflection trust-region chop's degenerate-at-boundary case (`chop = dist_to_inflection / |dsw|` -> exactly `0.0` when a cell's Sw sits essentially at the inflection point) — three variants tried: additive `max_saturation_change` margin, `dist.max(max_saturation_change)` floor, skip the chop below a `1e-4` degenerate-range threshold | `fim/newton.rs` (`appleyard_damping_breakdown`) | Heavy water 12x12x3/dt=1 (target: `water@387`/`cell129`'s "invalid bounded Appleyard candidate" failure), full control matrix | All three variants individually correct on a focused unit test and left the 5 non-target control-matrix cases unaffected, but each regressed the heavy case substantially (`62→263`, `62→263`, `62→238` substeps) — and each regression brought back the just-fixed `perf@1299` oscillation (`FIM-NEWTON-006`) rather than a new failure. **Root cause of the sensitivity found (task #37):** `cell115`'s zero damping and the entry-guard/zero-move-acceptance thresholds (`1e-8`/`2e-8`, deliberately far below the `1e-5` residual tolerance per `FIM-NEWTON-004`) are each individually legitimate, but both are *single global scalars* applied to the whole Newton update — loosening either lets the *entire* step move further that iteration, including at whatever other site is marginal then (`perf1`). Confirmed the retry ladder itself is correct, deterministic behavior (residual scales quadratically with `dt` across 5 retries until crossing the entry-guard threshold, discovering a genuine local Sw plateau, not a bug). **Note (task #38): this was, in retrospect, re-treading the same axis as the already-documented `FIM-DAMP-002`/`003` k-sweep — should have been cross-referenced before spending 3 live-test cycles here** | REFUTED | A per-cell/per-region damping or acceptance criterion (a materially larger architectural change than a chop-formula tweak) — not another single-scalar-damping variant at this site | `docs/FIM_CONVERGENCE_WORKLOG.md` "Step 11.4 (continued)", "Task #37" |
| FIM-DAMP-004 | Newton damping | Re-sweep `FW_INFLECTION_OVERSHOOT_FACTOR` (`k`) under the current Phase 10/11 linear-solver bundle, per `FIM-DAMP-003`'s own retry condition (linear solver changed materially since the April sweep: loosened tolerance/budget, block-ILU0, well elimination) | `fim/newton.rs` (`FW_INFLECTION_OVERSHOOT_FACTOR` constant) | Heavy water 12x12x3/dt=1 across `k ∈ {1.0, 1.1, 1.15, 1.2, 1.25, 1.3, 1.5, 2.0}`, full control matrix, locked smoke, **plus 2026-07-06 fine-dt FOPT reference** (`--steps 16 --dt 0.0625`, the skipped April-methodology gate) | `k`↔substeps is genuinely chaotic (`1.0`→248, `1.1`→32, `1.15`→214, `1.2`→62, `1.25`→32, `1.3`→32, `1.5`→204, `2.0`→134) — a Newton-trajectory bifurcation, not a smooth trend (`1.15` sits between two good values). `[1.25, 1.3]` is a genuine stable plateau (bit-identical trajectories at both), unlike the isolated single-good-point risk at `1.1` or the bad spot at the old sweet spot `1.2`. Promoted `k=1.25`: heavy case `62→32` substeps, control matrix bit-identical, locked smoke 3/3; new dominant retry site (`water@1215`) confirmed to be the same benign local-plateau mechanism from task #37, not a new failure. **2026-07-06 fine-dt accuracy check (task #38 continued) found a real cost**: current-bundle `k=1.25` fine-dt FOPT (`oil` @ step 16) = `3883.47`, vs. OPM converged reference `3826.12` (+1.50%). Isolated by rerunning the identical fine-dt command at `k=1.2` under the *same* current bundle: FOPT = `3845.38` (+0.50%) — so the bundle itself (tolerance/budget/block-ILU0/well-elim, none of which this row touches) already costs ~0.5% vs. the April tight-tolerance match (`3826.36`, +0.01%), and `k=1.25` specifically adds a further ~1.0 pp on top of that. Substep-count win (`62→32`) is real; it is not accuracy-neutral | PROMOTED, accuracy caveat added 2026-07-06, **user accepted the ~1.5% drift as immaterial 2026-07-06** given the far larger overall gap to OPM's ~2.5-iter/step target — do not re-tune `k` on this basis alone | Re-sweep again only if the linear-solver bundle changes materially (matching this row's own trigger condition), or if new evidence shows the drift growing/blocking something else. Before any further `k` change for a *different* reason, still require a fine-dt FOPT check in the same pass as the substep sweep | `docs/FIM_CONVERGENCE_WORKLOG.md` "Task #38", "Task #38 (continued)" |
| FIM-BUNDLE-N | Nonlinear layer (whole) | Replace acceptance/damping/timestep control with OPM's shipped semantics as ONE flag-gated bundle: MB+CNV convergence criteria (volume-avg `1e-7` global + local `1e-2` with relaxed tier and 3%-PV exemption), per-cell `ds-max`/`dp-max-rel` chopping replacing the global damping scalar + inflection chop, `pid+newtoniteration` timestep controller replacing the retry ladder/hotspot machinery, OPM linear-failure handling, deletion of the compensating-mechanism stack | `fim/newton.rs`, `fim/timestep.rs` | End metrics ONLY (never per-mechanism vs old-architecture baselines): fine-dt FOPT gates, heavy-case Newton-its ≤35 (today 336), ≤1 cut (today 13 rungs), locked smoke + BL benchmarks | Motivated by the 2026-07-07 factor budget: 738x wall-clock gap to OPM = 30.5x Newton-count (nonlinear architecture) x 24x per-iteration cost. **Checkpoint 1 (2026-07-07, inert CNV/MB measurement, no-op gated)**: criteria-swap alone saves ~0 iterations on current trajectories — CNV never binds (0/44 blocks), OPM's MB `1e-7` binds in 32/44 because the global-scalar-damped Newton stalls at MB≈`2e-6` at local plateaus. The 30x lives in the update/damping dynamics (N2) + retry ladder (N3); N1 alone would regress. Build order reordered: N2 first. **Checkpoint 2 (2026-07-07, `OpmAligned` flag + N2 per-cell chopping, default-Legacy no-op gated)**: informational `--opm-aligned` heavy run is worse end-to-end under Legacy gates (226 substeps, expected for the intermediate), but the MB stall is fixed — 95% of solve attempts reach a full-OPM-rules-acceptable state vs 48% under Legacy damping (median stall MB `2.2e-6 → 2.9e-7`); the Legacy acceptance layer is now the fragmentation source. **Checkpoint 3 (2026-07-07, N1 acceptance + N5 linear handling, default-Legacy no-op gated)**: a real gating bug (two more ungated Legacy acceptance shortcuts + the post-loop exhaustion check) was caught by a small-case smoke run showing zero new-mechanism traces firing, fixed before trusting any number. Informational runs: `22x22x1` worse than Legacy (14 vs 4 substeps, expected); heavy case timed out >280s (not chased live — known-mismatched intermediate, `MAX_SUBSTEPS=100_000` means slow not hung). N3 (controller) is now load-bearing for heavy-case tractability, not optional. **Checkpoint 4 (2026-07-07, N3 `pid+newtoniteration` controller, default-Legacy no-op gated)**: informational `22x22x1` run is WORSE than checkpoint 3, not better (20 substeps/12 retries vs 14/7) — OPM's flat `solver-restart-factor=0.33` retry backoff can't escape the stubborn `oil@415` site as fast as Legacy's repeated-hotspot acceleration (down to `0.2`) did; open question carried into N4/end-metric evaluation. **Checkpoint 5 (2026-07-07, N4 mechanism deletion, default-Legacy no-op gated) — FIRST REAL WIN**: deleted `candidate_materially_changed` validity requirement (confirmed dominant cause, 11/12 of checkpoint 4's failures), the residual-stagnation trend bailout, and the preemptive direct-solve bypass ladder. `22x22x1` informational run: `12 substeps/1 retry` (was `20/12`), `DAMPING FAILED` `11→0` exactly as predicted, now close to Legacy's `4/2`. NOT yet resolved: heavy case still times out `>400s` even at `--diagnostic summary`; a third case (`23x23x1`) surfaced a NEW `linear-bad` (not `nonlinear-bad`) failure mode at `retry_dom=linear-bad:oil@361` — N5 may need a second look before §5. **Checkpoint 6 (2026-07-07, N5 bug fix)**: the `linear-bad` finding was a genuine bug, not a design gap — N5's reduction check read `failure.outer_residual_norm`, which can stay pinned at `rhs_norm` on a non-converging solve even after later restarts genuinely improved (confirmed: every `23x23x1` failure reported `reduction=1.000e0` regardless of magnitude); fixed to use `FimLinearSolveReport::final_residual_norm` (the true candidate residual, matching Dune ISTL's own `result.reduction`). `23x23x1`: `26/9` → `12/1` attempts, `LINEAR-ACCEPT` now correctly fires. Heavy case still times out — its dominant failure is `nonlinear-bad` (Task #37's plateau), untouched by this fix. **§5 end-metric evaluation (2026-07-09) — FAILED, root cause identified**: ran the heavy case natively in `--release` (wasm runner was inconclusive); result `18,002 substeps` (gate: `≤35`), `176m25s` wall-clock — a decisive, not marginal, failure. Root cause verified against both the ResSim trace and the OPM source directly: a producer well pinned at its BHP limit needs `iters=20` (the Newton cap) on its rate-vs-BHP complementarity residual every substep once steady state is reached; N3's iteration-count growth formula (checkpoint 4) penalizes this as "physics moving too fast" (`growth=0.4` repeated, compounding to `0.4^N→0`). Confirmed OPM resolves well-control switching via a *separate inner iteration* (`WellInterface::iterateWellEquations`) invisible to the outer count that feeds its timestep controller — ResSim's flat single-level Newton loop has no such split, so porting the growth formula literally over-punishes well-switching cost. Independently re-derives "Hypothesis A" from the original Phase 8/9 well-coupling investigation via a different angle (controller, not linear solver). Does NOT indict N1/N2/N4/N5 (each showed real wins in isolation on `22x22x1`/`23x23x1`) — narrow, scoped defect in N3's formula specifically. **Follow-up (2026-07-09), two fix attempts, both ruled out**: (1) "decouple well iteration count from N3" — ruled out by code inspection BEFORE implementing: N1's acceptance already excludes wells and is the only accept path under `OpmAligned`, so the count fed to N3 is already reservoir-only by construction, nothing to decouple; (2) well-BHP update chop (OPM's `--dbhp-max-rel`, ported verbatim from `StandardWellPrimaryVariables.cpp::updateNewton`) — implemented, gated, tested natively on the heavy case: **`accepted_substeps=18002`, bit-identical to the unfixed run** (`min_dt`/`max_dt` match to the same float bits) — the chop never engaged, BHP is not the oscillating variable, hypothesis refuted. Two well-reasoned fixes ruled out (~5 hours combined compute); untested candidates: the (deliberately unchopped, OPM-matching) perforation-rate variable, or ResSim's own `relax_well_state_toward_local_consistency` post-processing step (no OPM counterpart, never examined). Recommend building cheaper diagnostic visibility (trace-to-file for the specific late-time window) before a third live guess | **REWORK REQUIRED — do not promote as-is** (checkpoints 0-6 done; flag off by default; §5 failed; 2 follow-up fixes ruled out) | Full design: `docs/FIM_BUNDLE_N_DESIGN.md` §5.1. On promotion, annotate superseded rows (`FIM-NEWTON-004`, `FIM-DAMP-002/003/004`, `FIM-NEWTON-007`) as pre-Bundle-N verdicts | `docs/FIM_CONVERGENCE_WORKLOG.md` "Task #41 (2026-07-07)", "Bundle N checkpoint 1/2/3/4/5/6", "Bundle N §5 end-metric evaluation (2026-07-09)", "Bundle N §5 follow-up" entries (2026-07-09) |
| FIM-BUNDLE-P | Linear/preconditioner cost | OPM-style CPR setup reuse (`cpr-reuse-setup=4`, interval 30) instead of full rebuild every Newton iteration; conditionally (per P0 measurement) LU-factor the ≤512-row coarse block instead of explicit dense inverse | `fim/linear/gmres_block_jacobi.rs`, `fim/linear/mod.rs`, `fim/linear/well_schur.rs`, `fim/newton.rs`, `fim/linear/solver_lab.rs`, `fim/linear/capture.rs` | Offline-first (P0: build-cost breakdown + stale-setup inflation study on captured corpora, gate: median ≤+2 iterations at staleness ≤30); P1/promotion NOT attempted, gate failed at P0 | P0.1 confirms the premise on the heavy case: `coarse_factorization_ms` (explicit dense inverse) = 97.5% of build time (48.67ms of ~50ms median), 25-500x every other phase. P0.2 fails decisively on both corpora: median inflation is fine (+1 iter) but new-convergence-failure rate is nonzero already at `k=1` (bounded 2.2%, heavy 0.2%) and climbs ~monotonically to `k=30` (bounded 10.3%, heavy 5.7%) — no staleness distance is failure-free | REFUTED (P0 offline gate failed 2026-07-10, both corpora corroborate) | Retry only if a materially different reuse-invalidation scheme (not a fixed `k`-interval) is proposed, e.g. residual-trend-triggered rebuild rather than iteration-count-triggered — the failure-at-k=1 result rules out any fixed-interval variant, including the recorded mode-1/mode-2 fallback levers (mode-1 ≈ no reuse since even k=1 fails; mode-2's >10-iteration guard doesn't gate on the k=1..10 failures already present) | `docs/FIM_BUNDLE_P_PLAN.md` "P0 results (2026-07-10)"; origin `docs/FIM_CONVERGENCE_WORKLOG.md` "Task #41 (2026-07-07)" |
| FIM-LINEAR-011 | Linear/CPR coarse solve | Lower `PRESSURE_DIRECT_SOLVE_ROW_THRESHOLD` `512→300`, moving the heavy case (432 coarse rows) and `22x22x1` (484) off the explicit dense inverse onto the already-production BiCGStab+ILU0 coarse path | `fim/linear/gmres_block_jacobi.rs` (threshold constant; new `coarse_factorization_lab_compare` lab helper), `fim/linear/solver_lab.rs` (new comparison test) | Offline 3-way on 599 recaptured systems (dense-inverse baseline / LU / BiCGStab); full control matrix + heavy case + locked smoke + BL benchmarks (live); heavy-case fine-dt FOPT (physics gate) | Split the conflated `coarse_factorization_ms` timer (`FIM-BUNDLE-P`'s P0.1) into `dense_inverse_ms`/`coarse_ilu0_ms`: dense inverse alone is 400x the ILU0 setup, confirming it (not ILU0) is the cost. Offline: BiCGStab converges on 599/599 captured systems (0 failures), reduction ratio `~4e-7`, 100-170x cheaper than the dense inverse (LU is only 4.3-4.4x cheaper, strictly dominated). Live: control matrix bit-identical on all 5 non-heavy cases incl. the newly-flipped `22x22x1`; heavy case wall-clock `36.9s→6.8s` (5.4x); heavy-case substep count DID shift (`32→52`, new "mixed" retry class) — expected given this system's known chaotic sensitivity (Task #37, the `k`-sweep), not itself a regression signal; fine-dt FOPT `3847.59` vs OPM's `3826.12` (+0.56%) — **better** than the currently-accepted bundle's own +1.50% (`FIM-DAMP-004`'s `3883.47`) | PROMOTED (kept live) | Candidate follow-up, not required: re-sweep `k=1.25` under this new coarse-solve config given the new "mixed" retry class — deferred per the user's 2026-07-06 guidance not to chase small accuracy deltas | `docs/FIM_CONVERGENCE_WORKLOG.md` "Coarse-factorization cost lever (2026-07-10)" |
| FIM-LINEAR-012 | Linear architecture / `OpmAligned` diagnostics | Two findings from Bundle Y Y1: (1) added an `OpmAligned`-side capture hook (`fim/newton.rs`, mirrors the existing `!opm_aligned` one) since the offline solver lab had zero evidence from this flavor before now; (2) found the likely mechanism behind the gas-rate `linear-bad` storm — `well_schur::solve_with_well_elimination`'s reduced (well-eliminated) solve returns `converged=true` at `reduced_iters=0` for some captured systems even though the resulting full-system solution is effectively zero (`full_residual_norm ≈ rhs_norm`); the wrapper's own safety-net check correctly downgrades this to `converged=false`, but forwards `failure_diagnostics: reduced_report.failure_diagnostics` unchanged — which is `None` because the reduced report claimed success — so `OpmAligned`'s no-fallback abort path (`newton.rs` ~line 3726) can never compute a `reduction` value and hard-aborts unconditionally, regardless of how good the true state was | `fim/newton.rs` (new capture call), `fim/linear/well_schur.rs` (`FIM_WELL_SCHUR_DEBUG` diagnostic print, both additive/no-op-gated) | Fresh `OpmAligned` corpora captured on current tree: heavy (1 system — confirms the heavy case's problem is not linear-solve failure), bounded `23x23x1` (1 system), gas-rate (337 systems, matching its live retry count exactly) | Restriction-variant lab (`solver_lab_compare_restriction_variants`) on the fresh 337-system gas-rate corpus: `quasi-impes` wins decisively (converged 336/337, wins 220/337, median rel. residual `1.13e-4` — 5-6x better than every alternative) — confirms the already-live restriction choice is in fact still correct for `OpmAligned`, not stale evidence. Full-dispatcher backend comparison (`solver_lab_compare_backends`, exercises the real `solve_linearized_system` path including well-Schur elimination) on the same corpus: **`fgmres-cpr` (today's exact live config) converges 337/337 offline, `iters=2` each, relative residuals `~2e-5`-`7e-5`** — every single system that was classified as a live failure solves cleanly when replayed in isolation with identical inputs and options. This is not a linear-solve-quality gap; the offline lab's own built-in "stop condition 2" assertion (designed for the old Legacy-flavor corpora, where near-0% offline convergence was expected) now fails loudly for the opposite reason — a genuine, useful signal, not a broken test. `FIM_WELL_SCHUR_DEBUG` windowed trace on the live gas-rate run found the specific pattern repeating: `reduced_iters=0` cases with `full_residual_norm` unmoved from `rhs_norm` | DIAGNOSTIC | Not yet fixed. Y1d (2026-07-12) closed the correlation gap exhaustively (programmatic check, not spot-check): all 337 gas-rate `LEDGER retry retry_class=linear-bad` lines are immediately preceded by a `WELL-SCHUR-DEBUG reduced_iters=0` line, itself immediately preceded by the matching `gmres_block_jacobi.rs:1651` preconditioned-residual accept event — one mechanism, zero exceptions, zero unexplained retries. Also discriminated the original two candidates and **both were wrong**: (b) elimination/recovery arithmetic is exact (refuted by code read, not just measurement — it's a textbook Schur complement independently checked by the very safety net that catches this bug); (a) tolerance-basis mismatch (reduced vs full `rhs.norm()`) is real but tiny (median 0.00%, max 3.69% relative difference across all 337 pairs) and cannot explain the observed 200x gap. **Actual mechanism**: `gmres_block_jacobi.rs:1651`'s `beta <= tolerance && family_ok(&residual)` check accepts on the *preconditioned* residual alone, evaluated at `iterations == 0` (one preconditioner application to the untouched `x=0` residual, before any Krylov step) — for these reduced systems `beta` lands 12.5x-31.2x under tolerance while the raw residual (what full-system quality actually depends on) sits a constant 200x over tolerance (`= 1/relative_tolerance`) every time. No raw-residual floor guards this accept path. Next-step candidate fix (not implemented, not measured): add a raw-residual sanity guard to the `beta`-only accept branch — but that branch is shared by every `FgmresCpr`/`GmresIlu0` solve in the codebase, not just well-Schur-reduced ones, so a wrong guard changes acceptance behavior far beyond this case; needs its own measurement pass first | `docs/FIM_OPM_PARITY_PLAN.md` §7-§8 (§8.6 for Y1d); `docs/FIM_CONVERGENCE_WORKLOG.md` "Bundle Y checkpoint Y1a/Y1b/Y1d" |
| FIM-LINEAR-013 | Linear/CPR accept criterion | Bundle Y Y1e fix for `FIM-LINEAR-012`'s pinned root cause: `gmres_block_jacobi.rs:1651`'s `beta <= tolerance && family_ok(&residual)` accept check (preconditioned-residual-only) fires at `iterations == 0`, before any Krylov correction has ever been applied — `solution` is still the untouched `x_0 = 0` guess at that point, so accepting there returns the zero vector as "the solution" purely because one preconditioner application crushed the preconditioned norm, regardless of whether `x_0 = 0` is actually close to correct. Fix: `iterations > 0 && beta <= tolerance && family_ok(&residual)` — generic, not well-Schur-specific; from `iterations >= 1` onward `solution` reflects a real Krylov step so the existing test is unchanged there | `fim/linear/gmres_block_jacobi.rs` (one-condition guard + new regression test `beta_only_accept_never_fires_on_the_untouched_initial_guess`) | Native `repro_gas_rate_20x20x3_opm_aligned_no_trace` (`--release`, the direct reproduction of `FIM-LINEAR-012`'s 337-system corpus); full bounded control matrix both flavors; Legacy + `OpmAligned`+`nested_well_solve` heavy cases; locked smoke 3/3 | **Live gas-rate**: `linear_bad` `337 → 1`, `nonlinear_bad` `0 → 4` (new, small, expected — Newton iterations now fed a genuinely-converged linear solve instead of a rejected one occasionally need a touch more nonlinear massaging), `accepted_substeps` `459 → 238` (48% reduction). `linear_bad` collapsing to near-zero directly confirms this defect was the dominant driver, not a coincidental correlation. **Control matrix, zero regressions**: Legacy `8/4/4/2` bit-identical, Legacy heavy (`dt=1`) `25` substeps bit-identical (same `retry_dom`/rung breakdown), `OpmAligned` bounded `16/24/12` bit-identical, `OpmAligned` bounded `12x12x3`+`nested_well_solve` `12` unchanged. The offline 337-system capture corpus could **not** be used for a before/after comparison — `solver_lab.rs::run_backend` builds `FimLinearSolveOptions::default()`, not the live Newton loop's actual options, so it never reproduced the live tolerance/`beta` relationship that triggers the bug (this also resolves `FIM-LINEAR-012`'s open "why did offline replay of live failures converge cleanly" question). Validated instead with a new synthetic unit test (two independent diagonal blocks, one with an inflated `1e6` diagonal) confirmed to fail without the guard and pass with it, plus the live re-measurement | PROMOTED | Does not close G2 alone (gas-rate `OpmAligned` still `238` vs Legacy's `2`) — retry/extend only if a differently-shaped accept-check gap is found (e.g. the same pathology recurring at `iterations >= 1`, not observed so far). **Y1f (2026-07-12) closed the "chase `nonlinear_bad=4`" follow-up**: all 4 come from one substep-0 retry ladder (not scattered), which before the fix hit the `linear-bad` bug on every one of its 7 retries (`mb=inf` throughout, `dt` shrunk to `0.000107`); after the fix the same substep gets real linear solves, converges close-but-not-quite within the 20-iteration Newton budget on 4 attempts (finite, shrinking `mb`), and accepts at `dt=0.002965` — 28x larger, 3 fewer retries. Confirmed benign, not a regression; no further action | `docs/FIM_OPM_PARITY_PLAN.md` §9 (§9.1 for Y1f); `docs/FIM_CONVERGENCE_WORKLOG.md` "Bundle Y checkpoint Y1e/Y1f" |
| FIM-BUNDLE-W | Nonlinear well coupling | Nested per-well inner Newton against frozen reservoir state, replacing `relax_well_state_toward_local_consistency` outright; inner solve drives the SAME assembled well residual rows (`well_constraint` + `rate_consistency` from `wells_ad.rs`); `dbhp-max-rel` chop inside (its correct OPM home); wells checked separately (closes N1's recorded fidelity gap). Independent flag (`nested_well_solve`), default off | `fim/wells_inner.rs` (new), `fim/state.rs` (`WellStateUpdateMode`), `fim/newton.rs` (flag, outer `wells_ok` gate), `fim/scaling.rs` (shared scale functions), `fim/timestep.rs`/`frontend.rs` (flag threading) | Heavy `12x12x3 --dt 1 --opm-aligned` §5 re-run vs baseline `17,990` substeps at `a362e29`; mechanism gate via `FIM-DIAG-002` WELLTRACE window; bounded `22x22x1`/`23x23x1` opm-aligned | **W0-W5 complete, 2026-07-11** (full detail: `docs/FIM_BUNDLE_W_PLAN.md` §6 + worklog "Bundle W checkpoint W0-W5"). **Mechanism check PASSED**: windowed trace on the real heavy-case trajectory confirms the diagnosed standoff is gone — `res_pf` machine-epsilon from iteration 1 (was floored ~5e-5), `q` stable to 8 decimals from iteration 1 (was never settling). **§5 re-run FAILED**: `18,015` substeps, essentially unchanged from the `17,990` baseline (within this case's known chaos-sensitivity) — **root cause: a second, previously-masked reservoir-side CNV plateau** (`cnv≈6.1e-5`, frozen across ~19 iterations, only clearing via the final-iteration relaxed tier) now drives the same `iters=20`/dt-collapse pattern the well standoff used to. Only 12 retries in the whole 18k-substep run (`oil@430`, non-well) — confirms the explosion is the accept-side dt cycle, not retries. Bounded cases mixed: `23x23x1` identical to `--opm-aligned` alone (genuine no-op, that case's bottleneck was never well-related); `22x22x1` regresses `12→24` (not root-caused with the same rigor). Fine-dt FOPT deliberately skipped (primary gate already failed decisively, mirrors Bundle N's own precedent) | **NOT PROMOTED — mechanism validated, kept inert** (same disposition shape as `FIM-BUNDLE-N`: real, independently-validated mechanism, insufficient alone because it exposed a second gap outside its scope) | Retry only after the newly-exposed reservoir-CNV plateau (see `FIM-DIAG-003`) has its own dedicated diagnostic pass and, if fixable, a fix — re-run W4's §5 gate with both in place | `docs/FIM_BUNDLE_W_PLAN.md` (§6 W4/W5); `docs/FIM_CONVERGENCE_WORKLOG.md` "Bundle W checkpoint W0-W5"; evidence `FIM-DIAG-002`; successor `FIM-DIAG-003` |
| FIM-DIAG-003 | Diagnostics / reservoir acceptance | Reservoir-side CNV/MB entry-criterion plateau under `OpmAligned`, exposed by `FIM-BUNDLE-W` fixing the well-side standoff that previously masked it. `cnv≈6.1e-5`/`mb≈1.4e-7` frozen across ~19 Newton iterations on a stuck heavy-case substep. Full D0-D5 diagnostic plan executed and closed 2026-07-12 (`docs/FIM_DIAG_003_PLAN.md`): unanimous verdict across three independent methods — **H1 (displaced standoff into well-cell MB rows) CONFIRMED** (D1 binding-cell census: 100% of frozen-MB iterations bind at the producer's own perforation cell 91% or its immediate neighbor 9%, unaffected by forcing exact linear solves; D3 OPM oracle: a well-posed Newton step transits the exact same MB magnitude in one clean iteration), **H2 (linear-precision floor) REFUTED** (D1: exact-solve rerun doesn't break the freeze, `advanced_dt` improves only 0.6%), **H3 (MB formula fidelity) REFUTED** (D2: line-by-line audit vs pinned OPM source `062cb1998`, no formula bug found; found and fixed an orthogonal off-by-one instead, `FIM-NEWTON-008`). D4 averted a false "win" (Legacy+`nested_well_solve` looks better on the raw ledger but regresses `real_accepted_substeps` `51→7`) and retracted a stale "`22x22x1` regression" claim. Mechanism precisely located → successor fix bundle `FIM-BUNDLE-X` (below) | `fim/newton.rs` (CNV/MB entry criterion, `cnv_mb_diagnostics`) | Heavy `12x12x3 --dt 1 --opm-aligned`; control matrix; D3 deck INFOITER | See summary above — full numbers in `docs/FIM_CONVERGENCE_WORKLOG.md` "FIM-DIAG-003 checkpoint D0"–"D5" | DIAGNOSTIC — CLOSED, unanimous verdict, superseded by `FIM-BUNDLE-X`'s fix | N/A — diagnostic closed; `min_strict_mb_iter` stays out of scope (H1 is a genuine structural fixed point, not slow convergence) | `docs/FIM_DIAG_003_PLAN.md`; `docs/FIM_CONVERGENCE_WORKLOG.md` "FIM-DIAG-003 checkpoint D0"–"D5" |
| FIM-DIAG-003-D3 | Diagnostics / OPM oracle | Trajectory-level differential comparison against real OPM Flow (`/usr/bin/flow`) on a tracked `water-heavy-step1` deck (`opm/reference-decks/water-heavy-step1/`, adapted from `water-medium-step1`, `DIMENS 12 12 3`, `TSTEP 1.0`) — does OPM's own MB genuinely drop below `1e-7` at states comparable to ResSim's frozen `1.4e-7`-`2.3e-7` plateau? | `opm/reference-decks/water-heavy-step1/CASE.DATA` (new, tracked) | `flow CASE.DATA --output-extra-convergence-info=steps,iterations`, `CASE.INFOSTEP`/`CASE.INFOITER` | OPM solves the entire `t=0→1.0` interval in **one Newton solve, 11 iterations, zero timestep cuts** (`0.03s` wall). Per-iteration `CASE.INFOITER` trajectory: iteration 10 has `MB_Oil=6.95e-7 MB_Water=1.97e-7` — the same order of magnitude ResSim is frozen at — then iteration 11 (final) drops to `MB_Oil=1.09e-9 MB_Water=1.13e-8`, a clean 2-3-order-of-magnitude single-step drop (`CNV` values drop `~250x` the same iteration — classic quadratic Newton tail). `WellStatus=CONV`/`PenaltyWellRes=0` throughout, no well-side distress. Oracle-side proof the `1e-7`-`2e-7` zone is not an inherent numerical floor — a well-posed Newton step passes through it — independently confirming `FIM-DIAG-003` H1 and reinforcing H2/H3's refutations | DIAGNOSTIC | N/A — pilot for trajectory-level differential comparison as a standing method (week retrospective §4); re-run once a scoped H1 fix exists as the verification instrument | `docs/FIM_CONVERGENCE_WORKLOG.md` "FIM-DIAG-003 checkpoint D3"; `opm/reference-decks/water-heavy-step1/README.md` |
| FIM-DIAG-003-D4 | Diagnostics / combination coverage | Two D4 combination checks: (a) Legacy + `nested_well_solve` on the heavy case, never run before — is it an independently promotable Legacy-side win? (b) re-derive the `22x22x1` OpmAligned+W "`12→24` regression" and `23x23x1` ride-along at current HEAD | `fim/timestep.rs` (`nested_well_solve`, plateau-replay bookkeeping) | Heavy `12x12x3 --dt 1` Legacy vs Legacy+W; `22x22x1`/`23x23x1` OpmAligned vs OpmAligned+W | (a) `accepted_substeps` ledger (`8` vs Legacy's `52`) looks like a win but is a measurement trap: `real_accepted_substeps` (the genuine Newton-solved count) drops `51→7`, with dt collapsing to `6.1e-5` after only 7 real attempts vs Legacy's ~50 — a much earlier, more severe stall masked by the plateau-replay bookkeeping collapsing a huge replay block into ~1 ledger entry either way (`51+1≈52`, `7+1≈8`). (b) `22x22x1` and `23x23x1` both bit-identical with/without `nested_well_solve` under `--opm-aligned` (`24`/`24`, `12`/`12`, matching `retry_dom`/`avg_p`/`oil`/`inj`/`gor`/`dt` bounds exactly) — the previously-recorded "`22x22x1` `12→24`" regression does not reproduce (most likely a stale/mislabeled reading; `23x23x1`'s own OpmAligned-alone number is `12`, which may be the source of the mislabel) | REFUTED (a); REFUTED — claim did not reproduce (b) | (a) Retry only if a different well-coupling mechanism than `nested_well_solve` is proposed for Legacy. (b) N/A — re-verify again if `nested_well_solve`'s bounded-case behavior changes | `docs/FIM_CONVERGENCE_WORKLOG.md` "FIM-DIAG-003 checkpoint D4" |
| FIM-NEWTON-008 | Newton | Fix confirmed off-by-one from `FIM-DIAG-003` D2: `OPM_NEWTON_MIN_ITERATION_INDEX` was `1`, should be `2` to match OPM's `NewtonMinIterations` default and `iteration() >= minIter` gate semantics (source-traced through `NewtonIterationContext`'s 0-based counter and `nonlinearIteration()`/`advanceIteration()` ordering — ResSim's own Newton loop has the identical assemble-check-update shape, so `iteration` is the direct analog of OPM's `iteration()`) | `fim/newton.rs` (single constant + doc comment) | Full control matrix + `--opm-aligned` variants of the 3 fast bounded cases (`20x20x3`/`22x22x1`/`23x23x1`) | Legacy/flag-off path: **bit-identical**, all 6 standard control-matrix commands (constant is only read inside `if opm_aligned`). `OpmAligned` bounded cases: `22x22x1` and `23x23x1` substep counts **unchanged** (`24`/`24`, `12`/`12`), only a tiny 4th-decimal `oil`/`inj` refinement from the mandatory extra iteration; `20x20x3` shows a small, expected **increase** (`15→16` substeps, `retries 4/1/0→6/1/0`) — the fix makes acceptance *stricter* (matching OPM's actual default, which forces more iterations than our previous floor), so an occasional extra substep is the correct direction, not a regression. Heavy-case pathology (frozen at iteration ~18-20) is provably unaffected by construction: the floor only gates iterations 0-1, far below where that case's failure occurs — not re-run for this fix (cost/benefit; the code-path argument is exact, not probabilistic). Locked smoke 3/3, `assembly_ad` parity 10/10 | PROMOTED | Retune only if a case's fast-converging-regime behavior is disputed with fresh evidence | `docs/FIM_CONVERGENCE_WORKLOG.md` "FIM-DIAG-003 checkpoint D5" |
| FIM-BUNDLE-X | Nonlinear well coupling / producer-fraction fidelity | H1 structural fix (successor to `FIM-DIAG-003`). X0 found the actual mechanism differs from the original well-update-ordering hypothesis: `perforation_control_cells`/`producer_control_state` (`fim/wells.rs`) fed a producer's phase-fraction mobility calculation a 3x3 areal neighborhood instead of the perforated cell alone (injectors already single-cell; OPM's `WellInterface::getMobility` uses only the connected cell for every well type, confirmed by source read) — a pre-FIM design (`git log -S "producer_control_state"` → `d824f4f`) mirrored unexamined into the AD layer. Near a pre-breakthrough producer (the heavy case's exact scenario), the neighborhood-blended fraction manufactured a spurious water withdrawal debited against a cell whose only effective lever (`dsw`, ~20x more sensitive than `dp`/`dq`) was legitimately clamped at the connate floor — the actual invariant-point generator; the well-update-ordering veto (`NestedSolve`) was real but secondary, not primary. X1 implemented the fix (flag-gated); X3 confirmed generality (Legacy flavor, a second independently-broken case, D3 oracle re-comparison showing the fixed trajectory reaching OPM's own `dt≈0.185`/`0.259`-class steps) and split the promotion decision (fix promotable now, the separate `OpmAligned`+`nested_well_solve`-as-default stack question spun off); X4 made it unconditional per user decision — deleting the flag surfaced and fixed a *second*, independent duplicate of the same bug in `producer_control_state` (caught immediately by the `assembly_ad`/legacy parity gate failing on the first post-removal rebuild) | `fim/wells.rs` (`perforation_control_cells`, `producer_control_state`), `fim/wells_ad.rs` (`producer_fractions_generic`), `fim/newton.rs` (X0 `WELLJAC`/`WELLJAC-WATER` trace) | Heavy `12x12x3 --dt 1`; control matrix; BL benchmarks; D3 deck INFOITER; `validate-solver-coverage.sh fim`+`shared` | Heavy case (Legacy, new unconditional default): `52 → 25` substeps; under `OpmAligned`+`nested_well_solve` (X1, behind the now-deleted flag): `18,015 → 16` (~1126x). Every other control-matrix case's substep count unchanged (only 4th-sig-fig production-number drift on cases with a producer near a front). Full gate green throughout: parity 10/10, locked smoke 3/3, `fim` bucket 9/9, `shared` bucket 14/14 (one confirmed pre-existing, structurally-unrelated failure skipped, `TODO.md` updated), BL benchmarks 3/3 unchanged | **PROMOTED, unconditional — no dev flag remains anywhere in the codebase** | Re-open only if `perforation_control_cells`'s call sites change, or to re-open the separate, still-open `OpmAligned`+`nested_well_solve`-as-default stack question (blocked on a pre-existing, unrelated bounded-case cost tradeoff, never this bundle's scope) | `docs/FIM_BUNDLE_X_PLAN.md`; `docs/FIM_CONVERGENCE_WORKLOG.md` "Bundle X checkpoint X0"/"X1"/"X3"/"X4" |
| FIM-DIAG-002 | Diagnostics / well coupling | Late-window native file trace sink (`fim/trace_sink.rs`, `FIM_TRACE_FILE`/`FIM_TRACE_DT_BELOW`/`FIM_TRACE_SUBSTEP_START`/`FIM_MAX_SUBSTEPS`) + per-iteration `WELLTRACE` line, to identify the actually-oscillating variable in Bundle N §5's 18k-substep heavy-case pathology without a multi-hour blind guess | `fim/trace_sink.rs` (new), `fim/timestep.rs`, `fim/newton.rs` | Heavy `12x12x3 --dt 1 --opm-aligned` | Re-baseline post-`FIM-LINEAR-011`: pathology persists, `17,990` substeps (vs prior `18,002`, same chaos-sensitivity shift as Legacy's `32→52`), wall-clock `298m13s→1288.7s` (~13.9x, confirms `FIM-LINEAR-011`'s cost-lowering framing). Windowed rerun (5 independent `iters=20` substeps inspected via `WELLTRACE`): BHP raw Newton correction is **exactly** `0.0` every iteration for both wells (strongest confirmation yet that BHP is not the culprit). The producer well's perforation rate is: raw (pre-relax) Newton correction settles into a non-vanishing per-substep plateau (`0.28`-`0.64` m³/day across the 5 substeps); `relax_well_state_toward_local_consistency`'s contribution tracks its near-exact negative every iteration (net movement ≈0, which is why CNV/MB and `q` look converged); `perforation_flow` residual plateaus at a nonzero floor scaling linearly with the same raw-correction plateau (ratio `≈8.63e-5`, consistent to 3 sig figs across all 5 substeps) instead of vanishing — a persistent **disagreement** between the Newton linearization and the relax step's independently-derived rate formula, not a classical 2-period oscillation (small iteration-to-iteration `res_pf` deltas are exactly why `FIM-NEWTON-006`'s OSC-DETECT widening can't and doesn't see this). Injector well unaffected (`~1e-7`/`~1e-12`, negligible) | DIAGNOSTIC | N/A — instrumentation stays in the tree (no-op verified: control matrix bit-identical, locked smoke 3/3, `assembly_ad` parity 10/10, wasm build green); re-run only if the well-pinned pathology's mechanism is disputed after Bundle W lands | `docs/FIM_CONVERGENCE_WORKLOG.md` "Late-window trace diagnostic on the 18k pathology (2026-07-11)" |
| FIM-Y1J | Diagnostics / OPM gas-rate injector isolation | Exact 10x10x3 first-rung driver with well/control selectors and forced-direct linear backend, to choose between linear/update and injector well/assembly hypotheses before a convergence fix | `fim/timestep.rs` (`repro_gas_rate_10x10x3_y1j`) | Both-rate live vs direct; no-well, injector-only, producer-only; both-pressure; `FIM_MAX_SUBSTEPS=1` | Live and exact direct have the same 5 retries, accepted `dt=0.000978384825`, 20 iterations and `res=1.040889e-5`; both trace the late-iteration plateau. No-well and producer-only accept full `dt=0.25`; injector-only and pressure-control rows fragment further. **Injector well coupling is necessary and sufficient on this first rung; rate constraint and iterative linear stack are refuted as primary causes.** Injector-only was clean-reconfirmed by Y2a; the full selector matrix remains the original diagnostic record. | DIAGNOSTIC | Continue through Y2 bound/update parity; do not retry stagnation acceptance widening | `docs/FIM_OPM_PARITY_PLAN.md` §13; `docs/FIM_CONVERGENCE_WORKLOG.md` Y1j |
| FIM-Y2A | Diagnostics / injector Jacobian and primary-variable audit | At the first injector plateau, compare AD, legacy, central FD, and one-sided FD entries for the perforation plus connected cell; read matching OPM `StandardWell` variables before a structural change | `fim/newton.rs` (test-only `FIM_Y2A_AUDIT`), `fim/timestep.rs` driver | Exact 10x10x3, injector-only and both-rate first-rung traces, `FIM_MAX_SUBSTEPS=1` | Clean-commit `5a600ae` replay: residuals agree to `1.137e-13`, but at `Sw=Swc=0.15` `d(res_pf)/dSw` is AD `-1428.56` vs legacy/backward FD `0`; forward FD is `-1428.56` and central FD is half. The Newton correction points below `Swc` and is projected back. Y2b0 confirmed raw-state/endpoint-property/in-loop-switching divergence. | DIAGNOSTIC — clean replayed | Y2b0 is complete; execute Y2b1 test-only characterization before selecting a derivative convention. No well-row-only/global-clamp/acceptance/G4 change | `docs/FIM_OPM_PARITY_PLAN.md` §§14-15.1; `docs/FIM_OPM_CONVERGENCE_EXECUTION_PLAN.md` |
| FIM-Y2B | State update / active bounds | OPM/ResSim saturation-bound policy divergence causes the exact injector plateau by making the Jacobian predict movement that ResSim projects away | `fim/state.rs`, `fim/newton.rs`, `fim/flash.rs`, `fim/properties.rs`, `fim/assembly_ad.rs` | Exact gas-rate first rung; complete tagged-primary lifecycle; exact historical capture; Sparse/dense/CPR replay; six-step Flow target; bounded controls; heavy/fine-dt/physics gates | Gates A-C close the partial-probe defect. Y2c matches Flow's six substeps but exposed `22x22x1=11/8L`. Y2d5 proves that regression was masked by the separate Krylov defect: combined result is `3/0L+1N`, while exact gas remains six/zero but moves to `9,6,5,5,4,4` versus Flow `7,5,4,3,4,3`; heavy remains 7 versus Flow 1. | **VALIDATED POSITIVE — DEFAULT-OFF, NOT WHOLE-STACK PROMOTED** | Preserve lifecycle during Y2d6 actual-Flow linear-lifecycle design; no acceptance/controller/well edits. | `docs/FIM_Y2B3_PRIMARY_VARIABLE_LIFECYCLE_DESIGN.md`; execution plan §§5.4-7.7; parity plan §§15.6-15.16 |
| FIM-Y2B2A | Diagnostics / linear report oracle | Y2b2's direct/live split cannot be judged while universal reduction depends on optional backend failure metadata | `fim/linear/mod.rs`, `fim/linear/sparse_lu_debug.rs`, `fim/linear/well_schur.rs`, `fim/newton.rs` | Focused synthetic report contracts; forced-direct exact first-rung driver with raw-state behavior absent | Report now carries RHS/final norms and `reduction()` independent of failure diagnostics. Sparse-LU finite non-strict, well-Schur original-system norms, and OPM relaxed acceptance tests pass; AD parity is 10/10. Forced-direct baseline emits no finite `reduction=n/a`. The shared script still hits its known unrelated closed-system assertion (`2` vs `1`); the broad FIM script did not complete in the harness, so it is not credited. | **COMPLETE — oracle contract repaired** | Execute Y2b2b: restore only the same native default-off probe and capture/replay its exact iteration-1 system. | `docs/FIM_OPM_CONVERGENCE_EXECUTION_PLAN.md` §§5.1-5.2; `docs/FIM_OPM_PARITY_PLAN.md` §§15.3-15.4 |
| FIM-Y2B2B | Diagnostics / corrected behavior replay | With a valid backend-neutral oracle, determine whether the prior 9.2x live raw-state improvement is shared by direct solving and whether the actual corrections agree | `fim/state.rs`, `fim/newton.rs`, `fim/linear/capture.rs`, `fim/linear/solver_lab.rs` | Exact gas-rate `dt=0.00898425` iteration-1 capture; capped live/direct first rung | One claimed live artifact (904 rows/5540 nnz, SHA-256 `c503…11561`) replays CPR at `4.830552e-3`; explicit Sparse LU returns zero BHP/rate/cell correction and reduction `1.0`. Direct's own later capture has 5626 nnz because it never applied iteration 0. | **COMPLETE — raw capture passed to Y2b2c; Y2b still inconclusive** | Closed by Y2b2c: classify the raw capture's singular structure, not a Sparse-LU tuning fix. | `docs/FIM_OPM_CONVERGENCE_EXECUTION_PLAN.md` §§5.2-5.3; `docs/FIM_OPM_PARITY_PLAN.md` §§15.4-15.5 |
| FIM-Y2B2C | Diagnostics / direct-oracle closeout | Is the exact raw-state capture failing in sparse conversion, factorization, or the matrix itself; can one independent direct method replay it? | `fim/linear/sparse_lu_debug.rs`, `fim/linear/solver_lab.rs` | Preserved `904x904` live artifact, SHA-256 `c503…11561` | Conversion succeeds; faer `sp_lu()` factorization and ordinary dense LU both reject the matrix. Structural audit: 0 empty rows/duplicates/non-finite/all-zero rows; **120 empty columns**, all cell-local gas primary variable 2, matching 120 missing/zero diagonal candidates. Test-only rank-revealing dense SVD has rank `784/904`, solves `J dx=rhs` to `1.101044e-12` relative full residual, and differs from CPR by max `1.657241e-2`. | **DIAGNOSTIC — COMPLETE** | Do not tune Sparse LU. Before another raw-state behavior run, source the coupled OPM phase-presence/primary-variable lifecycle and show how inactive fixed-layout gas unknowns are removed or constrained. | `docs/FIM_OPM_CONVERGENCE_EXECUTION_PLAN.md` §5.3; `docs/FIM_OPM_PARITY_PLAN.md` §15.5; worklog Y2b2c |
| FIM-Y2B3 | State lifecycle / dependency design | OPM's per-iteration composition-slot adaptation is required to retain a live gas-component dependency when raw saturation crosses phase presence | `fim/state.rs`, `fim/flash.rs`, `fim/properties.rs`, `fim/assembly_ad.rs`, `fim/newton.rs`, OPM `blackoilnewtonmethod.hpp` / `blackoilprimaryvariables.hh` | Source audit; tagged transition state machine; one-sided FD; mixed structural gate; exact traced capture; three-backend replay; capped first rung; Y2c/Y2d5 matrices | Gates A-C pass. Exact trace maps all 300 primaries to live columns; direct/CPR corrections agree. Y2d5 removes the separate masked water linear failures: combined `22x22x1` is 3 substeps versus Legacy 4. Exact gas remains six substeps but Newton is not yet at Flow; heavy remains 7 versus Flow 1. | **COMPLETE — MECHANISM VALIDATED, DEFAULT-OFF** | Hold lifecycle fixed during Y2d6. Do not let the separate linear-stack work refute it or promote the whole stack. | `docs/FIM_Y2B3_PRIMARY_VARIABLE_LIFECYCLE_DESIGN.md`; execution plan §§5.4-7.7; parity plan §§15.6-15.16 |
| FIM-Y2D0 | Diagnostics / bounded control linear oracle | The candidate's excess `22x22x1` cuts come from a localized full-system correction-quality failure at its first `linear-bad` retry, not from the validated primary lifecycle | `fim/linear/solver_lab.rs` diagnostic replay plus existing capture/report infrastructure; production solver held fixed | Exact first candidate `22x22x1` linear-bad artifact; production-faithful CPR and Sparse LU with common RHS/final full residual, reduction, finite status, reservoir/well partitions, and correction-family deltas | Confirmed on clean `2030996`: structurally clean `1456x1456` system, SHA `725c…786b`. CPR reproduces live 30-iteration failure at reduction `1.4411e-2`; Sparse LU factorizes and reaches `5.5463e-15`. Both finite; report/recomputed residuals agree; CPR residual is reservoir-only; correction deltas are pressure `181.498`, Sw `0.351096`, perf rate `1550.931`. Disabling well elimination also fails, so wrapper/recovery is not the sole cause. | **CONFIRMED — ITERATIVE CORRECTION-QUALITY GAP** | Execute Y2d1 offline only. Existing restriction clue (row0 4/8 versus quasi-IMPES 0/8) is not production-faithful because its helper bypasses well Schur/scaling; repair that comparison before any live flip. | execution plan §§7.1-7.2; parity plan §15.11; worklog Y2d0 |
| FIM-Y2D1 | Diagnostics / production-faithful CPR component isolation | The bounded failures are caused by a pressure-restriction mismatch that survives the real well-Schur/scaling wrapper, and one existing variant can resolve them without losing the current gas counter-control | Test-only `well_schur.rs` restriction injection and `solver_lab.rs`; production dispatch unchanged | All 8 Y2d0 artifacts and 5 clean-regenerated current gas artifacts through every restriction with production well elimination/scaling/block-ILU0/tolerance/budget; full-system norms/partitions/direct deltas | Restriction sensitivity is real but corpus-dependent. Diag-balanced solves bounded 8/8 (quasi 0/8; median direct delta `4.23` vs `508.9`) but gas only 3/5 (quasi 4/5), including two catastrophic misses. Sum-rows gas 5/5 but bounded only 2/8. All direct references and report/full residual contracts pass. Historical 337 gas corpus is unavailable/stale; clean `e143c19` regeneration reproduces 238 substeps and yields the 5 current artifacts. | **COMPLETE — TRADEOFF; NO UNIVERSAL RESTRICTION / NO LIVE CHANGE** | Execute Y2d2 with quasi-IMPES fixed: production-faithful smoother then Krylov-budget isolation on both current corpora. Do not combine levers or add a case selector. | execution plan §§7.2-7.3; parity plan §15.12; worklog Y2d1 |
| FIM-Y2D2 | Diagnostics / smoother and Krylov isolation | With quasi-IMPES fixed, bounded failures come from the block-ILU0 fine smoother and/or 30-iteration budget, and one existing configuration can resolve all 8 without losing any usable current gas artifact | Test-only well-Schur/lab plumbing only; production solver fixed | Block-ILU0/full-ILU0/block-Jacobi at fixed 30 iterations on 8 bounded + 5 gas artifacts; then 30/60/150 budgets for best no-regression smoother; full norms/partitions/direct deltas | Production block-ILU0 is best: at 30, all smoothers are bounded 0/8 and gas 4/5, while alternatives worsen medians. Block-ILU0 budget 60 resolves bounded 8/8 at iterations 31-32 and gas 5/5 (hard miss at 31); 150 is bit-identical to 60. All finite; full norms/partitions/direct comparisons valid. | **CONFIRMED OFFLINE COMPONENT CAUSE — NO PRODUCTION CHANGE** | Execute Y2d3 restart-boundary history/accounting audit. Do not promote budget 60: Flow stays within 20, so extra ResSim iterations diagnose a remaining approach gap rather than OPM parity. | execution plan §§7.3-7.4; parity plan §15.13; worklog Y2d2 |
| FIM-Y2D3 | Diagnostics / Krylov restart-boundary audit | The sharp 30-to-31/32 transition is either an iteration-accounting boundary or useful progress in the first post-restart cycle, distinguishable by comparable true/preconditioned residual histories | Test-only FGMRES/lab tracing; production solver fixed | Eight bounded artifacts plus hard gas artifact at effective 30/60, block-ILU0/quasi-IMPES fixed; per-iteration estimated/actual preconditioned and true full residuals; exact-dense and tighter-iterative coarse controls | Iteration 30 is a completed production-identical correction. First-cycle true residual plateaus near `1e-2` while the Givens estimate diverges from actual preconditioned residual by median `1.169e19`; restart reduces bounded residual by median factor `1.294e-4`. Exact dense pressure solve makes bounded `8/8` converge in one iteration, but tightening iterative coarse tolerance `1e-6 -> 1e-10` leaves bounded `0/8` and gas `4/5` at 30. Production constants restored. The named FGMRES path is a fixed left-preconditioned GMRES recurrence applied to an input-dependent iterative CPR map. | **COMPLETE — ALGORITHM-CONTRACT GAP CONFIRMED; NO PRODUCTION CHANGE** | Execute Y2d4 test-only true right-preconditioned flexible GMRES with stored `z_j`; fixed CPR components and budget 30 on both corpora. No live run, dense shortcut, tolerance/budget promotion, or AMG. | execution plan §§7.4-7.5; parity plan §15.14; worklog Y2d3 |
| FIM-Y2D4 | Diagnostics / true flexible-GMRES oracle | The Y2d3 loss comes from applying a fixed left-GMRES recurrence to input-dependent CPR; true right-preconditioned FGMRES will preserve the residual relation and solve all hard replays within 30 iterations | Test-only Krylov solver/lab path; production solver and CPR components fixed | Synthetic nonlinear-preconditioner contract; fixed-linear equivalence; 8 bounded + 5 gas artifacts at effective budget 30; true histories, full partitions, finite status, direct deltas | Synthetic controls pass. True FGMRES resolves bounded `8/8` in two iterations (production `0/8` at 30; median reduction `1.205e-8`, direct delta `1.641e-4`) and gas `5/5` in 1-3 (production `4/5`; hard gas passes at one, `3.813e-3`, direct delta `5.729e-6`). Estimate/true disagreement max is `1.04e-8` bounded and `8.48e-11` gas. Exact Flow DBG instead selects BiCGSTAB/CPRW true-IMPES/paroverilu0/one AMG coarse loop; OPM flexgmres exists but is optional. | **CONFIRMED OFFLINE — RESSIM CONTRACT FIX, NOT FULL OPM STACK; NO PRODUCTION CHANGE** | Execute Y2d5 default-off production-path integration of only the validated recurrence, then captured replay and live gates. Hold all CPR/nonlinear components fixed; do not claim literal OPM parity. | execution plan §§7.5-7.6; parity plan §15.15; worklog Y2d4 |
| FIM-Y2D5 | Linear solver / default-off true-FGMRES integration | The Y2d4 recurrence will remove the identified live linear retry class without changing CPR or nonlinear semantics | Production-capable recurrence behind explicit default-off option; old path retained for A/B | Synthetic dispatch/default gate; Y2d4 corpora; capped `22x22x1` water and `20x20x3` gas; exact gas target; Legacy and Y2 bounded/heavy matrices | Dispatch defaults old/off and promoted core exactly matches Y2d4 (`8/8` bounded, `5/5` gas). Legacy live substeps/retries remain stable; under Y2, `22x22x1` improves `11/8L -> 3/0L+1N`, other water linear retries become nonlinear without more substeps, heavy stays 7. Exact gas stays six/zero; reported residual-evaluation counts move `8,5,4,4,4,4 -> 9,6,5,5,4,4`. Y2d7 supersedes direct comparison of either sequence with Flow's applied-update count. | **CONFIRMED — VALIDATED DEFAULT-OFF CORRECTNESS PATH; NOT DEFAULT OR OPM-STACK PROMOTION** | Keep option and old default. Execute Y2d6 source-complete design for actual Flow BiCGSTAB/true-IMPES/paroverilu0/one-loop-AMG lifecycle; do not tune FGMRES or partially swap the outer method. | execution plan §§7.6-7.7; parity plan §§15.15-15.16; worklog Y2d5 |
| FIM-Y2D6 | Linear solver / actual Flow linear lifecycle | A coherent implementation of Flow's selected BiCGSTAB/CPRW lifecycle, rather than further FGMRES tuning, is required to close the remaining Newton-iteration gap | Source-pinned design, capture-v3 8+5 corpus, fixed component oracle, exact DUNE recurrence, native default-off live integration | Exact Flow 2026.04 + DUNE 2.11; true-IMPES; separated well outer/fine/coarse; `J_rr` paroverilu0; one-level direct coarse at/below `1200`; seven identities; raw-norm strict `0.005`, 20-pair BiCGSTAB; exact gas/heavy/Y2/Legacy live gates | D6a-c prove the coherent linear stack offline (bounded `8/8`, gas `5/5`). D6d live preserves six uncut gas steps. Corrected Y2d7 accounting gives prior/D6d applied updates `7,4,3,3,3,3 -> 9,4,4,3,3,3` versus Flow `7,5,4,3,4,3`: D6d and Flow both total 26, but D6d's per-step L1 mismatch is 4 versus prior 3. Heavy remains 7 substeps versus Flow 1. Y2 bounded and Legacy guards hold; dense setup is slower. IMPES has no analogous CPR gap. | **COMPLETE — VALIDATED DIAGNOSTIC, DEFAULT-OFF, NOT PROMOTED** | Do not tune this linear path. Y2d7 localized first divergence to the injector reservoir component source after update 1; execute bounded G4 source/rate-conversion parity with nested well solve and Y2 lifecycle fixed. | `docs/FIM_Y2D6_FLOW_LINEAR_LIFECYCLE_DESIGN.md`; execution plan §7.7; parity plan §§15.17-15.23; worklog Y2d6a-d/Y2d7 |
| FIM-Y2D7 | Diagnostics / source-comparable nonlinear trajectory | The first Flow/ResSim nonlinear divergence will distinguish linear lifecycle, well-row update, primary switching, and reservoir component-source hypotheses | Fresh Flow `INFOITER`/`INFOSTEP`; native per-evaluation trace; default versus existing nested-well solve; exact gas six-step target | Evaluation-0 CNV/MB anchor; applied-update versus residual-evaluation accounting; post-update well/perforation residuals, rates, binding cell and primary meaning | Initial metrics nearly coincide. After update 1, Flow remains well-converged with oil MB `1.8375e-3`; default ResSim changes injector `-500 -> -526.85` and reaches oil MB `4.311e-3` at cell 0. Nested solve holds `q=-500` and removes the well-row distress but leaves oil MB `4.311e-3`. Comparable updates are Flow `7,5,4,3,4,3`, Y2 `7,4,3,3,3,3`, D6d `9,4,4,3,3,3`. | **DIAGNOSTIC — G4 AUTHORIZED** | Audit injector-cell component source and Flow `StandardWell` rate/unit conversion after the first `ds-max` update. Hold nested solve, Y2 lifecycle, linear routing, acceptance, and controller fixed. G5 is not first; audit IMPES only for any shared source-physics defect found. | execution plan §7.7; parity plan §15.23; worklog Y2d7 |
| FIM-Y2D8 | Diagnostics / injector RESV component-source lifecycle | Flow's report-step-frozen surface-to-reservoir conversion, rather than only ResSim's post-update well relaxation, explains the first source mismatch at the injector cell | Flow `RateConverter`/`WellAssemble`/`StandardWell` source audit; native `WELLSOURCE` trace from the production helper; default and nested exact first-step replays | Deck `RESV 500`; evaluation 0/1 q, Bg, component source, dt row contribution, q-column; Flow lifecycle scope | Both sides agree initially at `-500/0.0065=-76,923.077 Sm3/d`. Flow holds the regional start-of-report-step coefficient through Newton. Nested ResSim retains q but changes to local `Bg=0.005219627`, source `-95,792.278` (`+24.5%`, `-4,717.300` residual-row excess); default q drift makes `-100,936.942`. The trace and source code agree on rate and Jacobian. | **CONFIRMED — COUPLED G4 MECHANISM; NO PARTIAL FIX** | Design G4a as one coherent default-off surface-rate / frozen-RESV-coefficient / connection / source lifecycle with a source-rate-control oracle. Do not freeze only the source, change G5, or alter acceptance/controller/linear routing. Audit IMPES only if shared conversion physics is changed. | execution plan §7.7 Y2d8; parity plan §15.24; worklog Y2d8 |
| FIM-G4A | Design / Flow gas-RESV injector lifecycle | A valid first port must replace rate primary, frozen conversion, control row, connection row, and source together; source-only `B_g` freeze is structurally invalid | `docs/FIM_G4_INJECTOR_RESV_LIFECYCLE_DESIGN.md`; Flow `StandardWellPrimaryVariables`, `WellAssemble`, `RateConverter`, `StandardWell` | Exact one-perf gas `RESV 500` evaluation-1 source/control/connection contract; AD/legacy/local-global gates specified before code | Flow primary is positive surface `u`; current ResSim q is negative reservoir rate. Required probe: `-q_res/Bg(cell)-u=0`, `Bg_ref*u-Qresv=0`, source `q_res/Bg(cell)`. `Bg_ref` is report-step regional; current `Bg(cell)` stays in connection/source derivatives; source is `-u` only once the perforation row converges. Nested q solve, retry lifetime, multi-perf allocation, BHP switching excluded. | **DESIGN COMPLETE — NO BEHAVIOUR CHANGE** | G4b0 only: explicit RESV control kind plus immutable default-off report-step reference context and unsupported-case guards/tests. No assembly, source freeze, IMPES, or live comparison. | design doc; execution plan G4a; parity plan §15.25 |
| FIM-G4B0 | Injector RESV / representation and report-step context | Before routing the coupled Flow lifecycle, the scoped control form and immutable report-step reference can be represented without changing an equation | `well.rs`, `fim/flow_resv.rs`, `fim/timestep.rs`; design §5 | Unit capture/disabled/retry-refresh/unsupported guards; well-control and AD parity; locked FIM, curated FIM, shared, and BL buckets | `WellScheduleControl::Resv` and native-only `FIM_FLOW_RESV_INJECTOR` capture one-perf gas `RESV` context. Reference is hydrocarbon-PV-weighted pressure plus one-region PVT `B_g,ref`; it is copied over retries and refreshed only after accepted state write-back. Guards reject absent/non-gas/non-injector/multi-perf/surface-target/BHP-limited/nested requests. Neither assembler reads it; no source, primary, connection/control row, IMPES, or convergence path changed. Focused and FIM gates pass. Shared bucket has three passing checks then its documented pre-existing closed-system `rate_history` assertion (`2` vs `1`); not attributed. | **COMPLETE — INERT, DEFAULT-OFF** | G4b1 only: shared pure AD/f64 residual helper for the specified connection, control, and source equations, with value/AD/FD gates. Do not route production assembly or run live convergence. | design §5; execution plan G4b0; parity plan §15.26; worklog G4b0 |
| FIM-G4B1 | Injector RESV / local residual contract | A single generic residual can preserve Flow's current-FVF connection/source derivatives while applying frozen `B_g,ref` only to the RESV control row | `fim/flow_resv.rs`; design §§4-6 | Two non-reference `B_g` fixtures; f64 values; local AD pressure/u columns; central FD | `flow_resv_injector_residual<S: Scalar>` returns `c_s`, perforation, control, and gas-source terms. Both fixtures are constructed with `c_s=u=76,923.076923`; source is `-u` there but its u derivative remains zero before enforcement. AD and FD retain the current connection/FVF pressure derivative in perforation/source; `dR_ctrl/du=0.0065`. No assembler, source helper, primary update, IMPES, or live convergence path calls it. | **COMPLETE — LOCAL CONTRACT ONLY, DEFAULT-OFF** | G4b2 readiness audit must inventory and gate all coupled AD/legacy/source/control/update/scaling/diagnostic/reporting routes before an atomic implementation. Do not partial-route an assembler or run a convergence result. | design §5; execution plan G4b1; parity plan §15.27; worklog G4b1 |
| FIM-G4B2 | Injector RESV / atomic-route readiness | A recognized RESV schedule must not execute the historical BHP/q lifecycle while the Flow u/source/control route is incomplete | `fim/timestep.rs`, `fim/state.rs`, `fim/wells*.rs`, `fim/assembly*.rs`; readiness audit | Valid one-perf gas/PVT/RESV FIM invocation; audited path inventory | Audit confirmed `physical_well_control` treats only `rate` as rate-controlled, so RESV would fall through to BHP/q. A pre-Newton block now validates/captures then returns with no time advance; regression test passes. The audit records all coupled state/control/AD+legacy/source/update/scaling/Schur/diagnostic/reporting/retry routes. | **COMPLETE — SAFETY BLOCK, NO LIVE ORACLE** | G4b2a designs one atomic implementation and gates. Keep the execution block until it can replace every listed q/BHP route together; do not interpret any live result before then. | `docs/FIM_G4B2_ATOMIC_ROUTE_READINESS_AUDIT.md`; execution plan G4b2; parity plan §15.28; worklog G4b2 |
| FIM-G4B2A | Injector RESV / atomic route design | A typed surface-u state plus one routed AD/legacy/update/scaling/trace package can remove RESV's historical BHP/q fall-through without creating an internally inconsistent partial probe | `docs/FIM_G4B2A_ATOMIC_ROUTE_IMPLEMENTATION_DESIGN.md`; G4b1 helper; G4b2 audit | Typed state initialization; complete AD/legacy/FD matrix parity; u-update exclusion; route-aware scaling; exact Schur recovery; evaluation-0/1 trace; default-disabled parity | The code-level design requires a typed `ReservoirConnectionQ`/`FlowResvGasSurfaceU` primary rather than a q-named value with changed units. It defines BHP/u initialization, exact row/column units, independent legacy derivative path, no-FB/no-q-relax/no-nested boundaries, and no-live gate sequence. The G4b2 execution block remains. | **DESIGN COMPLETE — NO BEHAVIOUR CHANGE** | Implement the entire atomic route and all non-live gates in one commit; remove the block only in that commit. A first capped no-retry live rung follows separately. | `docs/FIM_G4B2A_ATOMIC_ROUTE_IMPLEMENTATION_DESIGN.md`; execution plan G4b2a; parity plan §15.29 |
| FIM-G4B2B0 | Injector RESV / assembler-context scaffold | The selected G4 rows can be threaded through AD and legacy assembly without changing the historical default path | `fim/assembly*.rs`, `fim/flow_resv.rs`, `fim/state.rs`, `fim/scaling.rs`; worklog G4b2b0 | One-perf state initialization; selected AD/legacy full matrix parity; u-column FD; historical `assembly_ad` suite | The scoped fixture passes initialization, selected matrix parity, u-column FD, route scales, and historical AD parity. The native path remains blocked. Its tail value is still q-named and legacy shares generic AD property differentiation; a total-row pressure FD conflated source with accumulation. | **SCAFFOLD — INCOMPLETE, EXECUTION BLOCK RETAINED** | Migrate to a typed primary, extract independent analytic source linearization, isolate the source FD, then add Schur/trace gates. Do not run live RESV or claim Flow parity. | worklog G4b2b0; G4b2a design |
| FIM-G4B2B | Injector RESV / atomic typed route | Replacing the selected q slot and every coupled consumer as one typed BHP/u lifecycle removes historical BHP/q fall-through without weakening historical paths | `fim/state.rs`, `fim/assembly*.rs`, `fim/flow_resv.rs`, `fim/scaling.rs`, `fim/wells.rs`, `fim/newton*.rs`, `fim/timestep.rs`, `pvt.rs` | Typed initialization/access; independent analytic legacy Jacobian; complete AD/legacy/central-FD matrix; source/control zero columns; route update exclusions; scaling; direct/Schur equality; residual-only reassembly context; default-off production smoke; curated FIM/shared/IMPES gates | `Vec<FimPerforationPrimary>` replaces parallel value/kind storage. Historical consumers require reservoir q and the selected route requires surface u. Legacy uses an independent quotient-rule source derivative; its comparison exposed and fixed the PVT-table knot derivative oracle. Complete selected matrix FD/parity and exact Schur correction pass. Newton acceptance and basin probes retain RESV context, diagnostics/reporting derive connection q, and the pre-Newton block is removed. Non-finite Legacy fallback corrections are rejected before mutation while finite historical fallback corrections remain usable. Focused, locked FIM, and IMPES gates pass; shared coverage stops at its documented pre-existing closed-system history-length assertion. No exact-deck convergence claim is included. | **COMPLETE — DEFAULT-OFF EXECUTABLE ATOMIC ROUTE** | Live evaluation is recorded separately as G4b2c; do not reinterpret its omitted inner-well semantic as an atomic-route implementation failure. | G4b2a design; execution plan G4b2b closeout; parity plan §15.31; worklog G4b2b |
| FIM-G4B2C | Injector RESV / first live typed-route oracle | The atomic BHP/u route alone will keep the selected connection/source comparable to Flow after update 1 | Committed `9cdff9b`; exact gas first-step and six-step native trace; fresh Flow 2026.04 `INFOITER`/`INFOSTEP`; historical q control | Same Y2 raw-primary lifecycle; zero retry; evaluation-0/1 `u`, `c_s`, `R_perf`, control, source, CNV/MB; six-step applied and linear counts | Both ResSim routes remain six/zero and `7,4,3,3,3,3` applied updates; typed u moves linear work `61 -> 64` versus Flow `27`. At evaluation 1 typed oil MB improves `4.311e-3 -> 3.493e-3` toward Flow `1.8375e-3`, but gas MB worsens `2.482e-3 -> 4.526e-3` versus Flow `2.8814e-3`. Decisively, `u=76,923.077` while `c_s=133,639.380`, leaving `R_perf=56,716.303`; Flow well status is converged. | **INCONCLUSIVE — OMITTED u-COORDINATE INNER WELL SOLVE IS ACTIVE** | G4b3 only: design/implement the selected u-coordinate inner solve so evaluation 1 has `c_s≈u` and converged control/connection before comparing source. Hold G5, acceptance, damping, controller, linear lifecycle, BHP switching, and multi-perf allocation. | execution plan G4b2b live result; parity plan §15.32; worklog committed-tree comparison |
| FIM-G4B3 | Injector RESV / route-aware inner well coupling | Solving the selected frozen-reservoir rows in `(bhp,u)` after each outer update will restore Flow-comparable connection/source state without changing historical wells | Shared f64/AD selected evaluation; local `[R_ctrl,R_perf] x [bhp,u]` system; route-aware state recovery and OPM well gate | Away-from-convergence local/global residual/Jacobian equality; perturbed local convergence; mixed selected/historical well convergence; focused, FIM, shared, IMPES gates; committed exact one-step trace | Residuals are bit-identical to global assembly and well-column Jacobian entries agree within `1e-12`; mixed routes pass. From `4cdea38`, the full `0.25 day` trial accepts with zero retries and seven applied updates. Evaluation 1 changes `u/c_s/R_perf` from `76,923/133,639/56,716` to `76,923/76,923/-1.16e-10`; gas MB moves `4.526e-3 -> 1.737e-3` versus Flow `2.881e-3`, while oil MB stays `3.493e-3` versus `1.8375e-3`. | **COMPLETE — MECHANISM PASSED; NOT YET A SIX-STEP PROMOTION** | Execute G4b4 six-step comparison from the same committed mechanism. Hold all solver policy and broader well semantics. | G4 design §5; execution plan G4b3 result; parity plan §15.33; worklog G4b3 committed-tree result |
| FIM-G4B4 | Injector RESV / six-step live closeout | The complete scoped selected-well lifecycle will remain converged across report-step reference refreshes and materially close the six-step Flow iteration gap | Clean detached `653868e`; exact gas six-step trace; held Y2 raw lifecycle and solver policy; Flow/G4b2c/historical controls | Cuts/retries; applied and residual-evaluation sequence; per-step linear work; evaluation-1 selected rows; wall time | Six full steps, zero retries. Selected rows remain converged (`|R_perf|<=2.01e-9`, `|R_ctrl|<=5.69e-14`). Applied updates change `7,4,3,3,3,3 -> 7,3,3,4,3,3`, still total 23 versus Flow 26; per-step L1 distance worsens `3 -> 5`. Linear work improves `64 -> 61`, still versus Flow 27. Runtime `0.528-0.559 s` is unchanged versus prior `0.545 s` and Flow `0.08 s`. | **COMPLETE — STRUCTURAL PASS; NOT A CONVERGENCE PROMOTION** | Keep scoped route default-off. Before G5 or policy changes, obtain comparable OPM injector-cell reservoir partitions and audit oil/gas accumulation, flux, and matched source at evaluation 1. Missing OPM partitions make that audit INCONCLUSIVE, not a physics refutation. | execution plan G4b4; parity plan §15.34; worklog G4b4 closeout |
| FIM-G4C0 | Reservoir residual / OPM partition oracle | A term-by-term production-row oracle will identify the first comparable mismatch outside the now-converged selected well rows | Trace-only ResSim AD decomposition; exact OPM `release/2026.04/final` TPFA patch at `b82f21d`; injector cell 0, evaluations 0/1 | Independent accumulation/face/source sum must reconstruct each engine's assembled row; OPM rates multiplied by `21600 s` | Both reconstruction gates pass. At matched evaluation 0, oil/gas vertical fluxes agree within `0.011%`, but Flow gas source is `-20,312.5` versus ResSim `-19,230.77 Sm3`; this alone moves the total by `1,081.73 Sm3`. Evaluation-1 states then diverge, so their flux differences are trajectory evidence, not a same-state formula refutation. | **DIAGNOSTIC — FIRST MISMATCH PROVEN** | G4c1 only: align PVDG interpolation used by the RESV reference/source, then repeat the partition and six-step gates. | execution plan G4c0/G4c1; parity plan §15.36; worklog G4c0/G4c1 |
| FIM-G4C1 | Shared PVT / PVDG interpolation parity | OPM/ECL reciprocal gas-table interpolation explains the evaluation-0 RESV source mismatch | `pvt.rs`; reciprocal `1/Bg` and `1/(Bg*mu_g)` interpolation; exact one-step/six-step gas oracle; shared/FIM/IMPES controls | Evaluation-0 partition; evaluation-1 partition; six-step applied/linear/time; AD/legacy/FD parity | Evaluation-0 source becomes exactly `-20,312.5 Sm3`; oil/gas vertical flux remain within `0.011%` of Flow. Six-step applied updates move `23 -> 25` (`9,4,3,3,3,3`) versus Flow 26, linear work `61 -> 62` versus 27, and runtime `0.528-0.559 -> 0.576-0.579 s` versus `0.08 s`. Correctness improves; speed does not. | **PROVISIONAL CORRECTNESS FIX — NOT A CONVERGENCE PROMOTION** | G4c2: compare the matched evaluation-0 Jacobian and first update. Do not interpret evaluation-1 face differences as flux-formula proof while states differ. | execution plan G4c0/G4c1; parity plan §15.36; worklog G4c0/G4c1 |
| FIM-G4C2 | Matched evaluation-0 Jacobian / first correction | The first divergent trajectory is caused by a reservoir/well Jacobian defect after the now-matched residual | Built-in Flow MatrixMarket export at exact `b82f21d`; explicit post-well-Schur materialization with `--matrix-add-well-contributions=true --linear-solver=cpr_quasiimpes`; ResSim full capture and direct solve; cell 0 with mapped equation/primary units | Same initial state, timestep, hydrocarbon primary meaning, reservoir rows, well-reduced matrix, RHS, and raw correction; reject exports without explicit well contributions | The historical comparison was not same-primary: ResSim's forced DRSDT0 classified initial `Rs=80` as saturated/`Sg`, while the Flow deck has no `DRSDT` and starts undersaturated/`Rs`. With redissolution enabled, mapped cell-0 residuals are effectively exact and dominant oil/gas pressure/Sw derivatives differ below `0.007%`; the direct ResSim `dRs=293.4539` agrees with the Flow matrix solve `293.4397` (`0.005%`). Smaller derivative gaps remain (water pressure `0.0006` vs `0.00135`; oil/gas Rs columns about `13.3%/3.1%`), but no G4 derivative patch explains the first categorical trajectory mismatch. OPM source confirms both engines switch an overshooting `Rs` primary to `Sg=0`. | **DIAGNOSTIC — COMPARATOR CONTRACT MISMATCH PROVEN; NO G4 PROMOTION** | G5a only: establish one explicit DRSDT contract for both exact-deck paths and compare the first post-switch evaluation. Keep broader well lifecycle, acceptance, damping, linear routing, and timestep control held. | execution plan G4c2; parity plan §15.37; worklog G4c2; `opm/diagnostics/README.md` |
| FIM-G5A | Matched primary-state configuration and first post-switch evaluation | With identical unlimited-redissolution semantics, the first post-switch divergence is caused by primary-variable adaptation | `flow_resv.rs`, `wells_inner.rs`; exact Flow/ResSim eval0-to-eval1 primary, well-row, source, and reservoir-partition traces | Both paths start with Rs, switch to `Sg=0`, and must retain the selected injector source; historical DRSDT0 route is a control | Adaptation already matches. ResSim's raw outer update instead put BHP below cell pressure; the clamped injector returned `q=0` with zero BHP sensitivity, so its selected local Newton solve could not recover. A scoped default-off pre-solve reconstruction using the same frozen total-mobility connection law restores `q=-485.932 rm3/d`, `u=81250 Sm3/d`, and source `-20312.5` per timestep. First-step residual evaluations improve `14 -> 9`; six-step matched counts are `[9,5,5,4,4,4]` evaluations, 59 linear applications, zero cuts, versus Flow `[7,5,4,3,4,3]` applied updates / 27 linear iterations. Historical DRSDT0 remains `[10,5,4,4,4,4]`. Dirty-tree runtime `0.588 s` versus Flow `0.08 s`: correctness/lifecycle gain, no speed claim. | **VALIDATED POSITIVE — SCOPED DEFAULT-OFF WELL-BRANCH RECOVERY; NO GENERAL G5 PROMOTION** | G4c3: explain the already-present evaluation-0 pressure/Sw correction difference from mapped derivative blocks. Keep full StandardWell lifecycle, PVTO/extrapolation, acceptance, linear policy, and controller held. | execution plan G5a; parity plan §15.38; worklog G5a; `opm/diagnostics/README.md` |
| FIM-G4C3 | Same-state pressure/Sw correction attribution | The mapped water-pressure and PVTO/Rs derivative gaps explain the remaining evaluation-0 correction difference | Exact matched ResSim sequence capture; test-only full-system sparse-LU counterfactual; Flow G4c2 mapped block; pinned OPM water/live-oil PVT source | Same RHS/full matrix and finite exact corrections; full-system residual reduction below `1e-10`; water-only, non-water-only, and combined variants | Water-only changes cell 0 `[dp,dSw,dRs]` from `[6.903753,-4.14225e-5,293.453882]` to `[6.837656,-9.23084e-5,293.453882]`; mapped non-water deltas give `[9.627512,-5.77651e-5,293.439646]`; combined gives `[9.469612,-1.27840e-4,293.439646]` versus Flow `[9.52669,-1.28610e-4,293.43965]`, with relative residual `2.12e-16`. Flow's water term is exactly the missing `PV*Sw*c_w`. OPM source uses inverse PVTO tables; ResSim uses direct Bo/mu interpolation and its exact fixture omits undersaturated branch rows. | **DIAGNOSTIC — DERIVATIVE BLOCKS EXPLAIN CORRECTION; NO PARTIAL PHYSICS PATCH** | G4c4 coherent PVTW/PVTO lifecycle, including reference pressure and all accumulation/flux/well/reporting/AD/legacy/shared consumers. A water-storage-only edit is unauthorized. | execution plan G4c3; parity plan §15.39; worklog G4c3; solver-lab counterfactual |
| FIM-G4C4 | Shared PVTW/PVTO lifecycle | The complete OPM-style water inverse-FVF and reciprocal in-table PVTO lifecycle will reproduce the G4c3 correction without a fitted diagonal | `pvt.rs`, shared/FIM consumers, exact eight-row PVTO fixtures; matched one/six-step traces; focused parity, solver, BL, WASM and product gates | First raw correction; evaluation-1 partitions; cuts, updates, linear applications and time; shared/browser controls | Raw cell-0 correction becomes `[9.740529,-1.314971e-4,293.4397]` versus Flow `[9.52669,-1.28610e-4,293.43965]`. Six full steps use 23 updates, 55 linear applications, zero cuts and `0.597 s`, versus Flow 26/27/`0.08 s`; the native gap falls below 10x to ~7.5x. Focused/IMPES/FIM/BL/product gates pass except the unchanged shared closed-system history mismatch. Browser water controls remain 8/4/4 and heavy improves 25->23; gas controls regress 2->4 and 14->28 substeps. | **VALIDATED POSITIVE — COHERENT IN-TABLE FIDELITY FIX; GAS CONTROL TRADEOFF** | G4c5 same-state remainder plus linear-cost audit. Keep G5, acceptance and controller held; reciprocal PVTO extrapolation remains missing. | execution plan G4c4; parity plan §15.40; worklog G4c4 |
| FIM-G4C5 | LeftExtreme PVTO derivative + bounded coarse setup cost | OPM's guided Rs interpolation explains the remaining correction, and the already-existing iterative coarse backend removes the measured 300-row dense-inverse hotspot without changing nonlinear semantics | `pvt.rs`, `gmres_block_jacobi.rs`, seven sequential exact captures, matched one/six-step traces, focused/locked/BL/FIM/IMPES/WASM/product gates | Exact first correction; evaluation-1 partitions; coarse residual reduction; updates, Krylov iterations, cuts and wall time; browser controls | Guided interpolation gives `[9.529566823,-1.286491521e-4,293.4396825]` versus Flow `[9.52669,-1.28610e-4,293.43965]`. Seven 300-row captures have zero iterative failures and maximum relative residual `7.879e-7`; routing exactly 300 rows to ILU0/BiCGSTAB keeps 23 updates/55 Krylov iterations/zero cuts and reduces six-step time `0.597 -> 0.191 s` (~2.4x Flow). Browser substep counts are unchanged from G4c4. | **VALIDATED POSITIVE — CORRECTION CLOSED; SETUP HOTSPOT REMOVED** | G4c6 must attribute 55 versus 27 linear-iteration semantics before any further solver policy change. Hold G5, acceptance, damping and controller. | execution plan G4c5; parity plan §15.41; worklog G4c5 |
| FIM-G4C6 | Linear-work count semantics | The apparent 55-versus-27 gap is caused by retry/report accounting, inner-well work, or repeated reservoir solve calls rather than Krylov efficiency | Explicit attempt-level counters in `newton.rs`/`reporting.rs`; exact gas six-step native replay; existing complete Flow-lifecycle counterfactual; fresh Flow `INFOSTEP` | Residual evaluations, applied updates, reservoir solve calls, Krylov iterations, retries and time as separate units | Exact gas ResSim is 29 evaluations/23 updates/23 solves/55 Krylov/zero retries; Flow is 32 evaluations/26 updates/27 Krylov. The nested well solve is state-update work. Therefore no duplicate solve or accounting defect exists: the remaining gap is 2.39 versus 1.04 Krylov iterations/solve. Full Flow lifecycle gives 28 updates/45 Krylov/`0.646 s`, slower than G4c5 and consistent with `FIM-Y2D6`. | **DIAGNOSTIC COMPLETE — COUNT DEFECT REFUTED; NO POLICY PROMOTION** | Prioritize water-heavy trajectory. Re-open exact-gas recurrence/preconditioner only with new same-preconditioner evidence outside Y2D4-D6. | execution plan G4c6; parity plan §15.42; worklog G4c6; `docs/SOLVER_COMPARISON_SUMMARY.md` |
| FIM-WATER-001 | Water-heavy oracle and mechanism split | FIM's 50 substeps and IMPES's 129 substeps share one timestep/physics restriction | Corrected Flow `COMPDAT` diameter; fresh Flow `INFOITER`/`INFOSTEP`; direct one-day FIM Newton probe; IMPES max-ΔSw sweep | Same-state evaluation-0 MB/CNV; full-target convergence; FIM residual trajectory; IMPES steps/output sensitivity | The old Flow deck passed ResSim radius `0.1` as Eclipse diameter `0.1`, producing the exact 1.23 PI/residual mismatch; diameter `0.2` restores evaluation-0 parity (Flow water MB/CNV `0.83078/358.90`, ResSim `0.8308/358.9`). Flow still takes one day in 11 Newton/13 linear iterations. Direct FIM dt=1 fails after 20 updates at injector `water@0`, MB `1.234`, with good linear reduction and an Sw `0.3↔0.5` cycle. IMPES steps scale `129→73→46→29` as max ΔSw changes `0.05→0.1→0.2→0.5`, with material output drift. | **DIAGNOSTIC — SHARED ROOT REFUTED; OPM ORACLE CORRECTED** | WATER-002: compare the matched evaluation-0 Flow/FIM Jacobian and raw first correction. Hold controller, acceptance, iteration cap and IMPES stability policy. | execution plan WATER-001; worklog WATER-001; corrected `water-heavy-step1` deck |
| FIM-WATER-002 | Matched water-heavy first correction | After matched evaluation-0 residuals, a reservoir derivative block causes Flow water MB to fall `0.831→0.314` while FIM remains `0.831→0.828` | Corrected Flow MatrixMarket evaluation 0 with explicit-well flag; Flow max-update diagnostics; ResSim sequential full capture; exact solver-lab projection/well-Schur attribution | Two-phase row/primary/unit/sign mapping; raw correction; applied chop; mapped Frobenius/RHS deltas; largest coefficients | Flow matrix solve gives injector `[dp=+110.703 bar,dSw=+160.361]`, independently confirmed by Flow `DP=1.107e7 Pa, DS=160.4`; ResSim gives `[-196.932,0.792745]`. Both apply `dSw=+0.2`, but ResSim's pressure safeguard applies `-90 bar`, reaching `p=210` and water MB `0.828`. The dominant mapped differences are ResSim's explicit BHP-well Schur saturation terms: injector water `17964.8` vs Flow `20`, producer oil `-18018.8` vs `-20`. Reservoir-only mapped matrix delta is `0.732%` versus `337%` after ResSim well Schur; RHS delta is `2.23%`. | **DIAGNOSTIC — FIRST TRAJECTORY SPLIT LOCALIZED TO WELL LINEARIZATION CONTRACT** | WATER-003: source-complete, default-off matched BHP-well linearization/update lifecycle. Do not disable coupling as a production fix; keep controller/acceptance/chops/limits/IMPES fixed. | execution plan WATER-002; worklog WATER-002; `solver_lab_water002_matched_first_correction`; OPM diagnostics README |
| FIM-WATER-003 | OPM endpoint-clipped saturation-property lifecycle | ResSim's large BHP-well Schur saturation terms come from a live Corey derivative at exact `Sw=Swc`, whereas Flow's SWOF property is constant-extended there | OPM `PiecewiseLinearTwoPhaseMaterial::evalAscending_`; ResSim scalar/generic derivative audit; native-only complete FIM mobility flag; first-update and full controlled replays | Property value/derivative, well coupling retained, injector applied pressure, evaluation-1 water MB, accepted substeps/retries/time, final output direction | OPM returns the endpoint value as a constant for `Sw <= first sample`. ResSim scalar derivatives already return zero at the endpoint, but generic AD kept `dkro/dSw` live. Freezing exact endpoint AD through reservoir and well mobility consumers changes the first injector state to `p=390, Sw=0.3` and water MB to `0.313756` versus Flow `0.31375`. The held controlled run improves `50 -> 10` substeps, `2 -> 0` retries and `4.39 -> 0.78 s`. A direct one-day run still fails the held 20-update cap; final FIM `FPR=374.09 bar`, injection `3249.99 Sm3/d`, oil `2935.16 Sm3/d` remain different from Flow `352.53/2996.32/2609.51`. | **VALIDATED POSITIVE — DEFAULT-OFF; FIRST SPLIT CLOSED, NO PROMOTION** | WATER-004: matched evaluation-1 matrix/RHS/raw correction and SWOF-vs-Corey interior contract. Hold limits/controller/IMPES. | execution plan WATER-003; worklog WATER-003; OPM diagnostics README |
| FIM-WATER-004 | Post-endpoint evaluation-1 linearization | The remaining direct-day gap is a solver/controller defect after the first update | Fresh Flow `nit_1` MatrixMarket pair; ResSim endpoint-replay sequence capture `00001`; exact well-Schur projection and direct solves | Matrix/RHS mapped norms; raw correction; fixed chops; Flow intermediate-state sufficiency; interior relperm derivatives | The mapped matrix/RHS deltas are `16.6253%/0.6280%`. Injector raw corrections are Flow `[dp=-38.3182 bar,dSw=+1.89389]`, ResSim `[-111.2297,+1.52877]`; both have the held `+0.2` saturation chop. Flow does not export the intermediate cell state, so exact same-state identity is not proven despite the matched global water MB. At `Sw=.3`, rounded SWOF slopes are `dkrw=.469`, `dkro=-2.031`, while analytic Corey is `.625/-1.875`; this remaining property-contract difference is sufficient to invalidate a solver-policy attribution. | **INCONCLUSIVE — ORACLE NOT STRICT SAME-STATE; NO POLICY PROMOTION** | WATER-005: replay rounded SWOF values/slopes through the complete FIM mobility/well lifecycle, default-off, then repeat this capture. Keep controller, limits, chops and IMPES fixed. | `solver_lab_water004_matched_second_correction`; execution plan WATER-004; worklog WATER-004 |
| FIM-WATER-005 | Rounded SWOF property lifecycle | ResSim's remaining evaluation-1 correction gap is caused by analytic Corey values/slopes rather than a solver-policy defect | Corrected deck's nine rounded SWOF rows; scalar/generic FIM mobility replay; endpoint first-update gate; fresh sequential capture and Flow `nit_1` matrix/RHS; controlled and direct held-policy replays | Table value and left-knot derivative; evaluation-1 MB/matrix/RHS/raw correction; fixed applied chop; direct cap; controlled work and output | At `Sw=.3`, replay gives `dkrw=.469`, `dkro=-2.031` and retains zero endpoint derivatives. Evaluation-1 water MB is `.313749997` versus Flow `.31375`; mapped matrix/RHS deltas become `.55813%/.62792%` from WATER-004's `16.63%/.628%`. Injector raw correction is `[-39.0118 bar,+1.93434]` versus Flow `[-38.3182,+1.89389]`, both chopping Sw to `.2`. Controlled work improves `10 -> 5` substeps and `.775 -> .281 s`; direct dt=1 still reaches the held 20-update cap but MB is `1.326e-6`. Final controlled outputs remain different from Flow. | **VALIDATED POSITIVE — DEFAULT-OFF PROPERTY PARITY; NO DIRECT-DAY PROMOTION** | WATER-006: obtain a backend-neutral final convergence/acceptance decomposition at the fixed policy; do not increase iteration limits, loosen acceptance, or alter IMPES. | `water_heavy_swof_replay_uses_rounded_left_segment_slope_at_point_three`; `solver_lab_water004_matched_second_correction`; execution plan WATER-005 |
| FIM-WATER-006 | Direct-day late-tail decomposition | The remaining 20-update miss is a controller/cap issue after property parity | WATER-005 sequential systems `14..19`; unchanged CPR and sparse-LU solves on each full captured system; native direct replay; existing late trace | Full RHS/residual/reduction, reservoir/well partitions, finite corrections and correction delta; chops/upwind/well residuals | At iter 14–19, state updates are unchopped; upwind flips reach zero at 18 and well/perf residuals are negligible. The water tail at cell 419 is monotonic, but CPR full-system reductions are `2.21e-3,1.56e-3,1.19e-3,1.06e-3,1.02e-3,1.01e-3`, versus sparse-LU `3.5–4.1e-15`. CPR/direct corrections differ `2.67% -> .618%`; direct live replay is worse (`MB=6.37e-5`) because this small mismatch changes the nonlinear trajectory. | **DIAGNOSTIC — CPR ACCURACY FLOOR LOCALIZED; NO CAP OR DIRECT-BACKEND PROMOTION** | WATER-007: source/measure CPR stopping and Schur-recovery full-system residual contract on this same tail. Hold nonlinear acceptance, controller, caps, chops, and IMPES. | `solver_lab_water006_late_tail_backend_neutral`; WATER-006 trace and direct control |
| FIM-WATER-007 | CPR stopping versus well-Schur recovery | The `~1e-3` full-system floor is caused by Schur recovery rather than CPR's stopping signal | WATER-005 iteration-19 capture; production-faithful CPR Krylov history recovered to full system; strict offline CPR control | Preconditioned estimate/actual norm, recovered full residual, full reduction, finite correction and snapshot/report equality | Default CPR returns `converged=true` at 3 iterations: estimate `5.454e-7`, actual preconditioned `5.469e-7`, but recovered full reduction `1.007e-3`. Strict `relative_tolerance=1e-8,max_iterations=60` reaches full reduction `3.860e-7` (still below that strict full target at 60). In both modes recovered snapshot true residual equals report, and WATER-006 already locates residual in reservoir rows. | **DIAGNOSTIC — RECOVERY DEFECT REFUTED; LOOSE PRECONDITIONED STOPPING CONTRACT LOCALIZED** | WATER-008: source/measure OPM's norm and stopping lifecycle and run only same-state CPR tolerance/budget sweep. Hold live nonlinear/controller/cap/IMPES policy. | `solver_lab_water007_cpr_stop_and_recovery_contract`; WATER-006 |
| FIM-WATER-008 | OPM stopping-norm and budget oracle | OPM's `.005`/20 recipe is semantically equivalent to ResSim's `.005`/20 CPR acceptance | OPM `setupPropertyTree.cpp` CPRW defaults; `FlexibleSolver_impl.hpp` Dune BiCGSTAB construction; WATER-005 iteration-19 capture; production-faithful well-Schur CPR sweep | Source lifecycle, full RHS/residual/reduction, preconditioned estimate/actual norm, finite correction, recovered snapshot/report equality | Flow maps `tol=.005,maxiter=20` to Dune's outer desired raw-residual reduction. ResSim can accept from re-applied preconditioned residual: production and `.005`/60 both stop at 3 iterations/full reduction `1.007231199e-3`. At 60 iterations, numeric `1e-4/1e-5/1e-6` reaches `1.931079858e-5/8.952550031e-7/4.200655241e-7` in `5/6/9` iterations. Thus budget is not limiting and equal numeric tolerance is not a shared contract. | **DIAGNOSTIC — STOPPING-NORM MISMATCH CONFIRMED; NO LIVE POLICY PROMOTION** | WATER-009: default-off raw-full-residual acceptance probe with same-state report-contract tests; do not change numeric tolerance, budget, nonlinear/controller/cap/chop or IMPES policy. | `solver_lab_water008_same_state_tolerance_budget_sweep`; OPM source listed in oracle |
| FIM-WATER-009 | Default-off raw full-residual acceptance | Replacing ResSim's preconditioned/family acceptance with Flow's raw `.005` criterion materially improves the WATER tail | Default-false CPR raw-accept mode; WATER-005 captures 14–19; production well-Schur recovery and Krylov history | Raw RHS/residual/reduction, reservoir/well partitions, finite correction, recovered snapshot/report equality | The mode suppresses preconditioned/tiny-tail acceptance and the extra ResSim family gate. All tail captures accept in 3 iterations with full reductions `2.493e-3,1.561e-3,1.194e-3,1.058e-3,1.018e-3,1.007e-3`, each below raw `.005`; well residuals are zero. The criterion is valid but deliberately loose, so it predicts no direct-day improvement. Flow's StandardWell-reduced operator and nonlinear update lifecycle remain unimplemented/mismatched. | **DIAGNOSTIC — RAW CONTRACT MATCHED IN ISOLATION; LINEAR-ACCURACY FIX REFUTED; NO LIVE A/B** | Source and compare Flow nonlinear acceptance/update/chop/primary-adaptation lifecycle on the matched water path. Keep numeric linear tolerance, budget, controller, cap and IMPES fixed. | `solver_lab_water009_raw_full_residual_acceptance_contract`; WATER-008 source oracle |
| FIM-WATER-010 | Flow nonlinear lifecycle/order | A missing nonlinear acceptance, chop, or primary-adaptation ordering explains the water tail | Flow `NonlinearSystem*`/`BlackoilNewtonMethod` source; fresh Flow 2026.04 INFOITER; native direct OpmAligned+SWOF+nested-well trace | Source order/tolerances; evaluation-indexed MB/CNV, wells, chop, primary state, post-update divergence | Flow checks MB/CNV and wells before solve; after a non-converged solve it updates/chops/adapts primaries and reassembles. Existing OpmAligned matches this scope. Flow and ResSim are close at evals 0–2, but after the second update Flow CNV oil/water is `41.516/50.531` while ResSim is `62.31/27.72`; ResSim subsequently reaches the unchopped cell-419 tail. The two-phase fixture has no primary switch and clean wells. INFOITER metrics do not prove identical intermediate state. | **DIAGNOSTIC — LIFECYCLE-ORDER DEFECT NOT SHOWN; FIRST POST-UPDATE DIVERGENCE LOCALIZED** | WATER-011: compare exact second raw/applied correction and post-update state/matrix representation. Hold linear `.005`/20, acceptance, controller, caps, damping and IMPES fixed. | Flow `NonlinearSystem_impl.hpp`, `NonlinearSystemBlackOilReservoir_impl.hpp`, `blackoilnewtonmethod.hpp`; fresh WATER-010 trace |
| FIM-WATER-011 | Second raw/applied update and matrix boundary | The eval-3 split is caused by linear policy, controller, caps, damping, or IMPES rather than water assembly representation | Flow `nit_2` MatrixMarket pair; held WATER-005 evaluation-2 capture; explicit-well Schur reduction; exact two-primary/unit/sign projection and direct solves | Mapped matrix/RHS norms, raw corrections, saturation-cap check, availability of post-update Flow state | RHS differs `0.771731%`, but the mapped physical matrix differs `12.969764%`. Injector raw Flow/ResSim corrections are `[+67.5900 bar,+.140264 Sw]`/`[+69.8011 bar,+.137713 Sw]`, both below the `.2` Sw cap; producer water correction differs `.000246`/`.000693`. Flow does not export intermediate primaries or applied pressure update, so exact post-update state is unavailable. WATER-012 also establishes that a strict Flow reduced-well oracle needs a same-policy materialized dump. | **DIAGNOSTIC — STATE/WELL ORACLE INCONCLUSIVE; NO LIVE POLICY PROMOTION** | Add the source-complete Flow observation oracle before attributing remaining matrix terms; keep gas control and all runtime policy fixed. | superseded lab probe by `solver_lab_water012_eval2_reservoir_well_decomposition`; WATER-010 trace |
| FIM-WATER-012 | Evaluation-2 reservoir/well assembly decomposition | ResSim well Schur is the source of the remaining matrix mismatch | Held WATER-005 evaluation-2 capture; raw reservoir block versus ResSim well-Schur reduced system; Flow `nit_2` materialized diagnostic export | Relative raw/reduced/Schur norms, ranked matrix entries, Flow policy compatibility | Reservoir-only delta is `40.786531%`; the Schur increment norm is `38.676838%` and reduces the final delta to `12.969745%`. The leading remaining entries are reservoir saturation derivatives with zero Schur term. Flow `system_cpr` rejects materializing well contributions; `cpr_quasiimpes` can export them but changes the flow solve policy/state. | **DIAGNOSTIC — RESSIM SCHUR IS CORRECTIVE; FLOW WELL ORACLE INCONCLUSIVE; NO LIVE POLICY PROMOTION** | Add an observation-only Flow dump of a materialized copy while retaining matrix-free `system_cpr`; then compare reservoir and well terms at the same evaluation/state. Keep all ResSim/IMPES runtime settings and gas control fixed. | `solver_lab_water012_eval2_reservoir_well_decomposition`; Flow `setupPropertyTree.cpp` compatibility check; WATER-011 |
| FIM-WATER-015 | Flow two-phase TPFA reservoir source audit | Flow intentionally omits exterior-cell mobility/PVT derivatives that ResSim retains | Flow `TpfaLinearizer::linearize_cell`, `BlackOilLocalResidualTPFA::calculateFluxes_`, storage source; ResSim paired-cell AD flux/storage; focused-pass regression | Global block orientation, upwind derivative ownership, component storage, ROCK porosity | Flow writes the focus-cell derivative to both residual rows and repeats the reverse orientation for the neighbor column. The two focused passes exactly equal ResSim's paired-AD blocks with rounded SWOF and gravity. Storage form matches; the quadratic-reference versus exponential ROCK difference is in pressure terms. | **DIAGNOSTIC — PARTIAL-DERIVATIVE HYPOTHESIS REFUTED; SAME-STATE ORACLE REQUIRED; NO LIVE POLICY PROMOTION** | WATER-016: source-replay Flow's first two applied updates, including pressure chop/primary adaptation, then compare reservoir terms at the reconstructed same state. No Flow rebuild. | `water015_two_focused_tpfa_passes_equal_paired_face_ad`; WATER-012 |
| FIM-WATER-016 | Flow applied-state reconstruction | Retained MatrixMarket pairs plus Flow update source can reconstruct the actual evaluation-2 state | `BlackOilNewtonMethod::updatePrimaryVariables_`; `NonlinearSystem::updateSolution`; retained `nit_0..2` MatrixMarket pairs; `solUpd_` output source | Presence of each per-cell applied correction and updated primary state | Source fixes the contract: subtract update; `dp-max-rel=.3`; per-cell two-phase `ds-max=.2`; no relevant primary switch. The artifacts contain only `J` and RHS. LU-solving them is not Flow's CPR correction; Flow exposes only maximum update magnitudes and writes no Newton-state payload. | **INCONCLUSIVE — REQUIRED STATE ORACLE ABSENT; NO LIVE POLICY PROMOTION** | WATER-017 only with an observation-only Flow primary-state/update dump from a compatible prebuilt binary, or explicitly authorized rebuild for that diagnostic. Never use LU-generated states as Flow trajectory evidence. | WATER-016 source audit; WATER-011/012 oracle limits |
| FIM-WATER-017 | Instrumented Flow applied-state dump | Flow's per-Newton applied correction and post-update primaries can be observed directly, supplying the same-state oracle WATER-011/012/016 lacked | `opm/diagnostics/water017-applied-state-dump.patch` on `BlackOilNewtonMethod::update_`; `opm/diagnostics/water017-build-flow-oilwater.sh`; `opm-simulators` worktree at `b82f21dba`; installed 2026.04 libs; `/tmp/opm-water-heavy-step1/CASE.DATA` | Stock-vs-built `CASE.INFOITER`/`CASE.INFOSTEP` equivalence with the dump disabled; then per-iterate applied correction and post-cap/post-chop primaries | Control PASSED: `CASE.INFOITER` bit-identical, `CASE.INFOSTEP` differs only in timing columns (`0/12/11/13/1`). Eleven iterates x 432 dofs dumped; semantics verified against `ds-max=.2` and `dp-max-rel=.3`. Iterate 2 = Flow `nit_2` unambiguously. Injector applied `+67.5816786 bar,+.140278419 Sw` matches WATER-011's LU value to `1.2e-4`; producer applied `+8.7498665 bar,+.000804216 Sw` versus LU `+7.38911 bar,+.000246153 Sw` is `18%`/`227%` off, so WATER-011's producer evidence compared ResSim to an artifact (ResSim `.000693354` is within `16%` of Flow's actual value). Two ABI traps documented: `HAVE_HDF5=1` required, `NDEBUG` must be absent. | **DIAGNOSTIC — ORACLE LIVE AND CONTROLLED; WATER-011 PRODUCER READING CORRECTED; NO LIVE POLICY PROMOTION** | Comparison half still missing: ResSim captures hold no primaries, so add a test-only ResSim per-iterate state dump, then compare at evaluation 2 under the fixed decision rule. Never read a dump whose control run was not recorded. | `docs/FIM_CONVERGENCE_WORKLOG.md` "WATER-017 instrumented Flow oracle"; `opm/diagnostics/README.md` |
| FIM-WATER-018 | WATER-017 same-state comparison | The evaluation-2 divergence is a state difference, so the WATER-011 `12.97%` matrix delta reflects a water-side representation defect | Flow dump from `water017-applied-state-dump.patch`; new `#[cfg(test)]` `dump_water017_ressim_state` in `fim/newton.rs` gated on `RESSIM_WATER017_DUMP_DIR`; held WATER-010 direct replay (`FIM_WATER_FULL_TARGET_PROBE` + endpoint/SWOF replay + nested wells) | Per-cell post-update `Sw`/pressure agreement per update; raw correction agreement; proximity of WATER-012's dominant cells to SWOF breakpoints | States MATCH at the assembly state (post-update-1): `dSw` max `1.34e-4`, median `2.83e-7`, rms `7.85e-6`; `dp` max `0.973` bar, median `0.657`. Trajectories separate only at update 2 (`max|dSw| 1.1e-1`). WATER-012's three dominant entries (cells 144/157/13) all STRADDLE a SWOF breakpoint; 13 and 157 sit at `Swc=0.1` where Flow's `evalAscending_` derivative is exactly zero below the endpoint, so the two engines carry zero versus finite relperm derivatives at the same cell from a `3e-7` state difference. 6/432 cells straddle. Separately, Flow's pressure exceeds ResSim's in 432/432 cells by `0.287-0.973` bar (mean `0.656`), not cleanly proportional to `p - p_ref`. ResSim update-2 injector correction `+69.800262 bar,+.137712734 Sw` vs Flow `+67.581679 bar,+.140278419 Sw`; field-wide correction delta `3.57` bar rms, `31.0` bar max. | **DIAGNOSTIC — STATE-DIFFERENCE HYPOTHESIS REFUTED; MATRIX DELTA ATTRIBUTED TO ENDPOINT-KINK AMPLIFICATION (INFERRED); PRESSURE BIAS OPEN; NO LIVE POLICY PROMOTION** | Prove or refute the kink attribution by reassembling ResSim's Jacobian at Flow's exact dumped post-update-1 state and comparing to Flow's `nit_2` export — now possible, previously not. Do not attribute the pressure bias to ROCK porosity without that test, and do not retune or select G4/G5/Y3 before it. | `docs/FIM_CONVERGENCE_WORKLOG.md` "WATER-017 same-state comparison"; WATER-011/012/015 |
| FIM-WATER-019 | Same-state Jacobian sensitivity probe | The evaluation-2 `12.97%` matrix delta is endpoint-kink amplification of a `1e-7`-scale state difference, not a representation difference | New ignored `water018_kink_amplification` (`fim/timestep.rs`) with extracted `water_heavy_12x12x3_fixture`; WATER-017 Flow and ResSim state dumps | Relative Frobenius change of ResSim's own reservoir Jacobian between its own post-update state and the same state with only `Sw`/pressure replaced by Flow's; same measure with SWOF-straddling cells ablated | Post-update-1 (`max dSw 1.337e-4`, `max dp .973` bar, 6 straddling cells): relative `dJ` = `1.2996e-1`, against WATER-011's cross-engine `1.2969764e-1`. Ablating the six straddling cells drops it to `4.240e-2`. Post-update-0 (no straddling cell) gives `4.398e-2`, the smooth background. Flow's assembler does not enter the experiment; hydrocarbon variable, regime and well state are held at ResSim's values. Norm conventions differ from WATER-011 (reduced projected system vs full reservoir block), so the claim is coincident magnitude, not three-figure identity. | **DIAGNOSTIC — STATE-SENSITIVITY CONFIRMED; REPRESENTATION-DEFECT EXPLANATION REFUTED; WATER-011/012 LINE CLOSED; NO LIVE POLICY PROMOTION** | Do not reopen the evaluation-2 matrix comparison; it is fully explained. Retry only if a future state gap is shown to move the Jacobian by materially less than the observed cross-engine delta. The surviving question is upstream: the one-signed `.287-.973` bar pressure bias in 432/432 cells. | `docs/FIM_CONVERGENCE_WORKLOG.md` "WATER-018"; WATER-011/012/015/017 |
| FIM-MB-001 | Convergence/correctness | The persistent oil material-balance floor (`~2.5e-6`) that stalls OpmAligned on areal water is the saturation front sitting on SWOF breakpoints, the same endpoint-kink mechanism as WATER-018; it is also the produced-oil gap | `relperm.rs` (`corey_table[_derivatives]`), `mobility.rs` (`fim_two_phase_relperm`), reservoir flux/accumulation (`fim/flux.rs`, `fim/properties.rs`); `FIM_W024_OIL_MB` decomposition hook | water 23x23x1 / 12x12x3 OpmAligned | Per-cell oil residual accum/faces/well-source split; well-share; knot-share (cells within one segment of a breakpoint) | On 23x23x1 limit-cycle substeps: `well_share=0.0000` (wells contribute nothing — WATER-023's well-coupling hypothesis refuted), `knot_share=1.0000` (the entire coherent net is front cells on SWOF breakpoints), `net~=abs_sum` (one-signed). Worst cells on the injector-corner front, `accum~=-faces` with ~4% residual. Heavy 12x12x3 converges because its front-cell kink errors CANCEL (`net`=`1%` of `abs_sum`, MB `2.6e-8`) where the areal single-layer front's align (MB `2.5e-6`). Legacy hides it by accepting the raw-converged state, which is why it is faster on areal water and why its oil differs. | **DIAGNOSTIC — ROOT CAUSE ATTRIBUTED: saturation-front SWOF-kink, NOT wells; same defect as the oil gap** | Fix: compare ResSim's tabulated value/derivative interval selection at a front cell against OPM's `PiecewiseLinearTwoPhaseMaterial` and make value+AD-derivative stay on one interval across an iteration so front cells stop limit-cycling. Weigh consistent one-sided knot evaluation, kink regularization, or OPM's CNV-only final-iteration acceptance. | `docs/FIM_CONVERGENCE_WORKLOG.md` "WATER-023", "WATER-024" |
| FIM-TIME-006 | Timestep control | OPM's PID+iteration-count accepted-substep growth is what lets OpmAligned take large steps, so giving it to Legacy should close the heavy-case gap | `fim/timestep.rs:1233` (`opm_accepted_step_growth_decision` gating), a temporary `fim_opm_timestep_growth` switch | water-heavy 12x12x3 dt1 | Substeps, dt range, growth limiter, per-substep Newton updates | Legacy with OPM growth got WORSE: `24` substeps/`4856 ms` -> `31`/`4267`, dt still `1.843e-2..5.528e-2` versus OpmAligned's `1.900e-1..3.600e-1`. Rung traces show Legacy needs `9-12` updates per substep at `dt=3.125e-2` while OpmAligned needs `5-9` at `dt=2.5e-1`, so the flavors differ in nonlinear acceptance, not in the growth controller. | **REFUTED — switch reverted, not left as dead configuration** | Only if a future change makes Legacy's per-substep Newton cost comparable to OpmAligned's at the same dt; the growth decision is not the lever on its own. | `docs/FIM_CONVERGENCE_WORKLOG.md` "WATER-022" |
| FIM-RELPERM-001 | Saturation functions | Evaluate relperm from a piecewise-linear table sampled from ResSim's own Corey curves, as OPM evaluates SWOF, instead of the analytic law | `relperm.rs` (`corey_table[_generic]`, `corey_table_derivatives`), `mobility.rs` (`fim_two_phase_relperm[_derivatives]`), `fim/wells.rs`, `fim/newton/damping.rs`, `frontend.rs` (`setFimCoreyTablePoints`), `lib.rs` (`DEFAULT_FIM_COREY_TABLE_POINTS = 21`), `scripts/fim-wasm-diagnostic.mjs` (`--corey-table-points`) | water-heavy 12x12x3 dt1, water 20x20x3 / 22x22x1 / 23x23x1 dt.25, gas-rate 20x20x3 dt.25, both flavors; Flow gravity-off deck as external reference | Substeps, retries, outer_ms, produced oil; knot sweep; single-step convergence; perforation residual | Plateau `13..33` (`n=21` centre). Native OpmAligned heavy `50` substeps/`4397 ms` -> `4`/`234 ms`; versus Flow's `~87 ms` that is `51x` -> `2.7x`. wasm OpmAligned heavy `55/6518` -> `4/833`. Legacy controls keep their substep counts with oil within `0.4%`; gas bit-identical. Produced-oil differences are TEMPORAL: at fixed substep count the spread across 41..385 knots is `<=1%`, while 4 vs 10 substeps moves it `5%`. Win survives at 257 knots (table-vs-Corey error `~4e-6`) and is UNCHANGED by the wells/damping consistency fix, so the mechanism is the piecewise-linear representation. On the direct one-day probe analytic Corey does not converge in 60 updates while the table converges in 20, matching Flow. | **PROMOTED 2026-07-23 (default `n=21`)** | Re-derive `n` from a fresh sweep if the linear solver or damping changes; substeps vs `n` is chaotic outside the plateau. The `rate_controlled_producer_fim_hits_bhp_limit` gate was made scale-aware (relative `<1e-5`, passed by both models) because near `Swc` linear interpolation of a quadratic legitimately over-estimates `k_rw` (`5.31e-9` vs `1.18e-10`) — OPM's own behaviour; do not read that as a defect. | `docs/FIM_CONVERGENCE_WORKLOG.md` "WATER-020", "WATER-021" |

Registry reconciliation note (2026-07-13): `FIM-LINEAR-005`'s long retry text preserves the Y1
discovery state, but its requested `OpmAligned` capture and live-dispatch replay were completed by
`FIM-LINEAR-012`/`013`. Quasi-IMPES remains promoted; restriction variants and AMG are not active
Bundle Y convergence work.

## How To Add A Row

Copy this template and keep it terse:

```md
| FIM-AREA-NNN | Area | Hypothesis / Lever | `file.rs` | target cases | measured result | VERDICT | required new evidence | source doc |
```

If the experiment changes behavior, the source doc must include:

- commit hash or clearly marked provisional dirty-tree status
- exact commands
- before/after summary lines
- validation controls that moved or stayed fixed
